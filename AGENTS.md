# مكتبتي الجيمرية — دليل استكمال العمل لأي AI

موجّه لأي وكيل ذكاء اصطناعي (Claude، GPT، إلخ) يفتح هذا المستودع ويحتاج يكمل التطوير.

**اقرأ قسمَي «القواعد الذهبية» و«أخطاء وقعت فيها فعلًا» قبل أي تعديل.** كل بند فيهما جاء
من باگ حقيقي حصل في هذا المشروع وكلّف وقتًا — أغلبها **يفشل بصمت** بلا خطأ في الكونسول.

---

## نظرة عامة

تطبيق تتبّع مكتبة ألعاب فيديو شخصية (PWA)، عربي بالكامل RTL، **بلا سيرفر خلفي إطلاقًا**.
كل البيانات محلية على جهاز المستخدم (IndexedDB)، والنسخ الاحتياطي يدوي كملف JSON.
مستضاف على GitHub Pages: `https://yasseralq90coder.github.io/YasirMaktabatiGaming/`

## هيكل الملفات

| الملف | الدور |
|---|---|
| `index.html` | التطبيق كامل (~11,340 سطرًا: React + منطق + CSS في ملف واحد) |
| `games_db.js` | قاعدة محلية لـ12,065 لعبة (أجيال 4/6/7) للبحث الفوري عند الإضافة |
| `manifest.json` | PWA manifest حقيقي (ليس Blob) |
| `sw.js` | Service Worker حقيقي — تخزين أوفلاين + تذكير العدّاد |
| `icon.svg` | أيقونة التطبيق |
| `tests/run.cjs` | 311 اختبار منطق — `node tests/run.cjs` |
| `tests/load-core.cjs` | يُحمّل سكربت `index.html` في بيئة معزولة للاختبارات |
| `AGENTS.md` | هذا الملف |

`.gitignore` يستثني نسخ المستخدم الاحتياطية (`game-library-backup*.json`) وملفات المسوّدات
(`scratch_*`) — **لا ترفع بيانات المستخدم الشخصية لمستودع عام**.

## القواعد الذهبية

1. **لا يوجد build step ولا JSX.** كل الكود بصيغة `React.createElement(...)` مباشرة.
   لا تُدخل JSX ولا أدوات تحويل ولا `package.json`.
2. **شغّل `node tests/run.cjs` بعد أي تعديل على المنطق.** 311 اختبارًا في ثوانٍ،
   وكل واحد يحرس خطأً وقع فعلًا. أضِف اختبارًا مع كل إصلاح جديد.
3. **تحقّق من الصيغة بعد كل تعديل** قبل الانتقال للتالي:
   ```bash
   awk '/^<script>$/{f=1;next}/<\/script>/{f=0}f' index.html > /tmp/x.js && node --check /tmp/x.js
   ```
4. **عند تغيير قيمة enum (نص)، ابحث عن كل مقارناتها الحرفية.** المشروع يقارن السلاسل
   حرفيًا في عشرات المواضع، وتغيير واحد بلا الباقي **يفشل بصمت**.
5. **لا تكرّر منطقًا موجودًا — استخرجه.** المسارات المزدوجة كانت أكبر مصدر باگات هنا
   (راجع الأخطاء ٢ و٧ أدناه).
6. **اختبر في متصفح حقيقي بـ`http://`** لا `file://` (الـSW وIndexedDB يحتاجانه).
7. **أي رقم يُعرض في مكانين يأتي من دالة واحدة، وأي شاشة تفصيل تجمع إلى الرقم
   الذي تشرحه.** الأخطاء ١٤ و١٥ أدناه كلاهما من كسر هذه القاعدة.
8. **الأثر الذي يتبع الحالة يُربَط بالحالة** لا يُستدعى من كل دالة تعدّلها (خطأ ١٣).

## اختبار التغييرات

**ابدأ دائمًا بـ`node tests/run.cjs`** — 311 اختبارًا تعمل في ثوانٍ بلا متصفح ولا
اعتماديات. كل اختبار فيها يحرس خطأً وقع فعلًا في هذا المشروع.

```bash
node tests/run.cjs        # منطق: المِران، الرتب، الأركيد، RA، الترحيل، الإنجازات
```

تعمل بتحميل سكربت `index.html` في بيئة `vm` معزولة (`tests/load-core.cjs`) تعوّض
واجهات المتصفح وReact، ثم تستدعي المنطق الخالص مباشرة. **لا تُرندَر أي مكوّنات**،
فالاختبارات لا تغطّي الواجهة — تلك تبقى يدوية.

| # | الطريقة | متى |
|---|---|---|
| 1 | `node tests/run.cjs` | **بعد كل تعديل على المنطق** |
| 2 | فحص الصيغة (أدناه) | بعد كل تعديل على `index.html` |
| 3 | متصفح حقيقي بـ`http://` | لأي تغيير في الواجهة |
| 4 | فحص IndexedDB | عند الشكّ في البيانات |

**فحص الصيغة** (الملف بنهايات LF — لا تُضِف `\r?` بلا داعٍ):
```bash
awk '/^<script>$/{f=1;next}/<\/script>/{f=0}f' index.html > /tmp/x.js && node --check /tmp/x.js
```

**سيرفر ستاتيك** (الـSW وIndexedDB لا يعملان مع `file://`):
```bash
python3 -m http.server 8934
```

**لاختبار بيانات حقيقية**: الإعدادات → «📥 استعادة نسخة» → الصق نص JSON في المربع →
فحص → تأكيد. أسهل وأوثق من محاكاة رفع ملف عبر الأتمتة.

**افحص IndexedDB مباشرة** (مصدر الحقيقة): `indexedDB.open('gamelib_db',1)` → store
اسمه `kv` → المفاتيح نصّية (`gamelib:games`, `gamelib:timer`, `gamelib:unlocked`,
`gamelib:cover:<id>`) وقيمها **string JSON** (لازم `JSON.parse`).

### أضِف اختبارًا مع كل إصلاح

أي باگ تُصلحه يستحق سطرًا في `tests/run.cjs`. الملف مقسّم لخمسة عشر قسمًا
(الخبرة، الرتب، الأركيد، التواريخ، RA، الترحيل، الإنجازات، الاتساق) وأدواته
جاهزة: `game({...})` و`sess(day, hours, src)` و`eq/ok/noThrow`.

---

## نموذج بيانات اللعبة

```js
{
  id, name, platform,            // platform قيمة من PLATFORMS (لها family + era)
  hardware: "original" | "emulator",   // ⚠️ موروث — المصدر صار s.hw، راجع «محورا التشغيل والملكية»
  emuDevice: "pc"|"steamdeck"|"aynthor"|"console"|"nso"|"other",  // ⚠️ موروث — لا يُنقَل للجلسة
  emuDeviceOther,                // نص حر لو emuDevice==="other"
  displayType: "crt" | "hd" | "",// ⚠️ موروث — المصدر صار s.disp
  status: "completed"|"playing"|"library"|"shelved"|"backlog",
  type: "physical"|"digital"|"downloaded"|"subscription",
  arcadeMode: boolean,           // تختيم بالنتيجة لا بالساعات (تلقائي كل 3 ساعات)
  scoreAttempts: [{date, score, used?}],  // محاولات أركيد؛ used=استُهلكت في تختيمة
  nso: boolean, nsoSystem,       // يفرض hardware:"emulator" + emuDevice:"nso"
  raGameId,                      // معرّف RA — يُكتب بالربط اليدوي وحده
  ra: {                          // بيانات RA — تظهر فقط لألعاب المحاكي
    gameId, title, consoleName, numAchievements, numAwarded,
    numAwardedHardcore, completion, guideUrl, achievements: [...], syncedAt
  },                             // ⚠️ بلا وقت لعب — RA لا يمسّ الساعات إطلاقًا
  raHist: {                      // سجل التكرار الداخلي — راجع قسم RetroAchievements
    v, ach: { "<achId>": { times, hcTimes, dates, title } },
    masteries: [...], resets: [...], seededAt
  },
  genre, year, developer, remaster, rating, hours,
  dateAdded, dateCompleted, notes,
  sessions: [{ at, ms, src, note?, hw, disp? }],  // راجع «src الجلسة» و«محور التشغيل»
  playthroughs: [{ date, hours, n, score?, hw }],
  parts: [...], goals: [...],    // تختيمات فرعية (لا تدعم arcade)
  desc, wikiUrl, trailerId, trailerMp4, hasCover,
  crt: legacy — يُقرأ للترحيل فقط، لا تكتبه
}
```

### محورا التشغيل والملكية — الفصل الجاري ⚠️

**اكتمل على خمس مراحل.** الهدف: اللعبة الواحدة
تُلعب على المحاكي مرّة وعلى الجهاز الأصلي مرّة، والملكية سؤال مستقل عند الإضافة.

| المحور | السؤال | مكانه | المصدر |
|---|---|---|---|
| الهوية | ما اللعبة وعلى أي منصّة صدرت | اللعبة | `platform` — إحصاء فقط |
| الملكية | هل أملكها | اللعبة | **مشتقّ**: `isOwned(g)` من `type` |
| التشغيل | أين لعبتُها هذه المرّة | **الجلسة** | `s.hw` + `s.disp` |

**`isOwned(g)` مشتقّ لا مخزَّن.** `physical`/`digital` ⇒ مملوكة، و`downloaded`/
`subscription` ⇒ لا. دالة تامّة من `type`، فحقلٌ ثانٍ لنفس السؤال كان سينحرف
(القاعدة الذهبية ٧). الواجهة تسأل عن الملكية أولًا ثم تعرض تفصيلها.

**الملكية والتشغيل مستقلّان تمامًا** — 42 لعبة في مكتبة المستخدم على جهاز أصلي
بنسخة غير مملوكة. لا تشتقّ أحدهما من الآخر أبدًا.

**`emuDevice` لا يُنقَل إلى الجلسة** — بطلب المستخدم صراحةً: لا فرق عنده بين PC
وSteam Deck وAyn Thor والجوال وخدمة NSO، كلها «محاكي». (`g.nso` يبقى حقيقة
ملكية تُغذّي رفّ NSO ومساره، لا حقيقة تشغيل.)

**ثلاث دوال هي كل الواجهة**، ولا يُكتب `hw` خارجها:
- `legacySetup(g)` — الإعداد الموروث من `g.hardware`/`g.displayType`، أساس الترحيل.
- `lastSetup(g)` — آخر إعداد لعبتَ به فعلًا (**بالتاريخ لا بموضع العنصر**: إضافة
  تختيمة بتاريخ ماضٍ تُلحق جلسةً أقدم في آخر المصفوفة)، وإلا الموروث.
- `stampSetup(o, setup)` — يكتب `hw`/`disp`. `disp` على `original` وحدها.

`mkSess(dateStr, hrs, src, note, setup)` هي **المسار الوحيد** لإنشاء جلسة، وتضع
`hw:"original"` افتراضًا فلا تخرج جلسة بلا تشغيل. **أربعة مسارات تنشئ جلسات وثلاثة
تنشئ تختيمات — كلها تمرّ من هنا** (خطأ ٧). أي مسار خامس يجب أن يمرّ به.

`g.hardware` و`g.emuDevice` و`g.displayType` **بيانات موروثة لا يقرأها التطبيق**.
مرّت مرحلةً بثلاثة موصِّلات (`legacyHw`/`legacyDisp`/`legacyEmuDev`) ثم زالت كلها
حين صارت كل الأسئلة تُوجَّه إلى الجلسات. **القارئ الوحيد الباقي هو `legacySetup`**
داخل الترحيل، وهو ما يوسم به الجلسات القديمة عند التحميل والاستعادة.

⚠️ **الحقول تبقى في البيانات عمدًا.** حذفها من الملفات قرار منفصل لم يُتَّخذ:
حذفها الآن يعني أن استعادة نسخة قديمة لا تجد ما توسم به جلساتها. خطآ ٣ و١٠
كلاهما من حذف/تغيير مبكر. لا تحذفها قبل إصدار كامل بلا مفاجآت.

### أين يُسأل كل محور — والواجهة التي تنفّذه

| السؤال | متى يُطرح | المكوّن |
|---|---|---|
| هل تملكها؟ | عند **الإضافة** | `renderOwned` — زرّان ثم تفصيلهما |
| أين لعبتها؟ | عند **اللعب** | `PlaySetup` — الجهاز الأصلي / محاكي |

`renderOwned()` يسأل «هل تملك اللعبة؟» أولًا ثم يعرض تفصيل الجواب وحده: المملوكة
نسخةٌ ملموسة أو شراءٌ من المتجر، وغير المملوكة محمّلةٌ أو باشتراك. يُستعمل في معالج
الإضافة وفي نموذج التحرير الكامل معًا — تعريف واحد لا اثنان.

`PlaySetup({ platform, value, onChange })` يظهر **قبل بدء العدّاد وعند تأكيد الوقت**،
وافتراضه `lastSetup(game)` فلا احتكاك في الحالة الشائعة. صفحة اللعبة تحمل حالة واحدة
(`setupPick`) تغذّي كل مسارات الكتابة، وتُصفَّر عند تبديل اللعبة.
⚠️ المكوّن مُعرَّف قبل `E` في الملف — استعمل `React.createElement` صراحةً داخله.

**الفلاتر تسأل عن الواقع**: «🎮 لعبتها على» عبر `playedOnHw`، و«📺 تم اللعب على CRT»
عبر `playedDisp`. **لعبة لم تُلعب بعد لا تطابق أيًّا منهما** — وهذا صحيح لا خلل،
ولهذا صيغ النصّ «لعبتها على…» لا «تشغيلها». وسقط فلتر جهاز المحاكاة نهائيًا.

**شارة المنصّة** تعرض المنصّة (إحصاء ثابت) ثم رمزًا لكل طريقة لُعبت بها فعلًا،
وقد تعرض 🎮 و🧪 معًا — وهذا هو بيت القصيد من التغيير كله.

### المقاييس تقيس اللعب لا الاقتناء

كل ما كان يُقاس بالحقل المُعلَن صار يُقاس بالجلسات، **ونزلت أرقام معروضة نزولًا
مقصودًا ومُقرًّا**: «🧪 ساحر المحاكاة» 10 ← 6، و«📺 عاشق الكلاسيكية» 43 ← 35.
الفرق هو الألعاب المُعلَنة ولم تُلعب — والمسار صار «ما لعبتَه» لا «ما تملكه».
**الأوسمة المفتوحة لا تُقفل**: `syncUnlocks` يحفظ المفاتيح في `KEY_UNLOCKED`.

- «🕹️ بطل المحاكاة» = تختيمة موسومة `hw:"emulator"` (لا حالة اللعبة).
- «🔀 بالطريقتين» وسام جديد يحلّ محلّ «مخترق الكونسول» الذي كان يقيس جهاز
  المحاكاة — محورًا سقط. يُفتح بلعبة واحدة `playedOn(g).length === 2`.

**بوّابة RetroAchievements صارت الربط اليدوي وحده**: `raShows = raGameId && ra`.
وشرط الإتاحة صار **المنصّة** لا طريقة التشغيل — `raSupportedPlatform(plat)` من
`RA_ALIASES`. الشرط القديم فقد معناه حين صارت اللعبة تُلعب بالطريقتين، وهو يفرض
نفسه بالبيانات على أي حال: إنجازات RA لا تُكتسب إلا بلعبٍ على محاكٍ واعٍ بها.
نطاق «حدّث الكل» اتّسع من ألعاب المحاكي إلى ألعاب المنصّات المدعومة (48 لعبة).

### دمج نسختَي لعبة واحدة

⚠️ **المنصّة جزء من المفتاح — لا اقتراح عبر المنصّات.** الانقسام الذي تعالجه
الأداة سببه طريقة التشغيل، وهي لا تغيّر المنصّة. Splinter Cell على جيم كيوب وعلى
إكسبوكس نسختان اشتُريتا لجهازين، لا سجلٌّ انقسم. كان الاقتراح يشملهما ويكتفي
بتحذير نصّي «الاقتراح لا يعني التطابق» — والتحذير لا يكفي حين يكون الفعل نقرةً
واحدة لا رجعة عنها. الحارس الآن في الشرط، ومكرَّرٌ داخل `mergeGames` نفسها.

`mergeCandidates(list)` يقترح، و`mergeGames(target, source)` يضمّ، والزرّ في
الإعدادات. **اقتراح لا تنفيذ**: النموذج القديم أجبر المستخدم على إدخال اللعبة
مرّتين لو لعبها بطريقتين، لكن «Splinter Cell» على جيم كيوب وعلى إكسبوكس نسختان
حقيقيتان — فالقرار له. الدمج يجمع الساعات والجلسات والتختيمات (تُرقَّم من جديد)،
ويأخذ أقدم `dateAdded`، ويحفظ الملاحظتين معًا، وينقل ربط RA إن كان الهدف بلا ربط.
**`raHist` لا يُدمَج** — عدّادا تكرارٍ لنفس الإنجاز لا يُجمعان بلا تشويه.

### «غير مملوكة» ≠ «محاكي» — لا تشتقّ أحد المحورين من الآخر ⚠️

لعبة محمّلة على الهارد الداخلي وتعمل على **الجهاز الأصلي نفسه** تجربةٌ أصلية
كاملة لا محاكاة. الخانات الأربع كلها مأهولة في مكتبة المستخدم:

| | مملوكة | غير مملوكة |
|---|---|---|
| **الجهاز الأصلي** | 100 لعبة | **42 لعبة** |
| **المحاكي** | 1 | 9 |

وتلك الـ42 ليست هامشية: **43 ساعة من أصل 122 (ثلث النشاط)**، و**41 منها على
CRT**. اشتقاقٌ يعامل «غير مملوكة» كمحاكاة يهجّر ثلث النشاط إلى العالم الخطأ
ويُسقط أوسمة CRT — **بلا خطأ في الكونسول**. يحرسه أربعة اختبارات، خانةٌ لكل
حالة، وخامسٌ يتأكّد أن اللعبة المحمّلة على جهاز أصلي وشاشة CRT تبقى في عدّاد CRT.

**استعلام التشغيل** — «أين لعبتُها فعلًا؟» جوابه الجلسات والتختيمات:
`playedOn(g)` (الطرق المستعمَلة) · `playedOnHw(g, hw)` · `hoursOn(g, hw)` ·
`playedDisp(g, disp)`. **لا تستعمل الموصِّلات الموروثة لهذا السؤال.**

**الترحيل ثابت (idempotent)**: إعادة تشغيله على بياناته لا تغيّر شيئًا. اختبره
دائمًا بتشغيله مرّتين — الانجراف هنا فشل صامت.

### `src` الجلسة — يقرّر دخولها في كل الإحصائيات ⚠️

| القيمة | المعنى | تدخل الإحصائيات؟ |
|---|---|---|
| `"timer"` / `"manual"` / `"run"` | جلسة جلوس حقيقية | ✅ |
| `"ra"` (`RA_SESSION_SRC`) | **ملغى** — من استيراد RA المحذوف. يُزيله `raPurgeImported` | — |
| `"backfill"` | وقت تعويضي مُصطنع من `reconcileGamesTime` | ❌ مستثنى من كل شيء |

يوجد **~28 موضعًا** يفحص `s.src !== "backfill"` (المِران، السلاسل، الخريطة الحرارية، عدد
الجلسات، التحديات، الرحلة، الأوسمة…). أي جلسة توسمها `"backfill"` تصير **صفرًا في كل
شيء**. استخدم `isSitting(s)` بدل `src !== "backfill"` لأي وسام يعتمد **طول جلسة واحدة**.

### تاريخ الحقول (لفهم البيانات القديمة)

- `type`: `"disc"|"digital"` → `"physical"|"digital"|"downloaded"|"subscription"`
- `hardware`: `original|emulator` → (مرحلة وسيطة: `console_emu|pc_emu|nso` — **لا تعِدها**)
  → الحالي `original|emulator` + `emuDevice` منفصل
- `crt` (boolean) → `displayType`
- `ra.playtimeMin` → `ra.playtimeSec` → **محذوف** (أُلغي استيراد الوقت كليًا)
- جلسات `src:"ra"` و`RA_IMPORT_NOTE`: بقايا استيراد ملغى، يحذفها `raPurgeImported`
- **NSO ليست جهازًا أصليًا**: بطلب المستخدم صراحةً، تُشغَّل عبر محاكي نينتندو الرسمي،
  فأي `nso===true` يجب أن تكون `hardware:"emulator", emuDevice:"nso"`.

## نظام الترحيل (migration)

`migrateGame(g)` و`migrateGamesList(list)` هما **المصدر الوحيد** لتطبيع الحقول القديمة.
**يجب استدعاؤهما من مكانين معًا** (كان غيابهما من الثاني باگًا حقيقيًا):

1. `useEffect` تحميل التطبيق (من `KEY_GAMES`)
2. `handleRestore()` عند استعادة نسخة احتياطية

(`grep -n 'migrateGamesList(' index.html` يجب أن يعطي **3 أسطر**: التعريف + استدعاءان.
لو صارا استدعاءً واحدًا فقط، فهناك مسار لا يُرحّل بياناته.)

أي حقل جديد يحتاج ترحيلًا → **أضفه في `migrateGame` فقط** ويعمل بالمكانين تلقائيًا.

`migrateGamesList` تستدعي أيضًا **`raPurgeImported`** التي تزيل ما خلّفه استيراد RA
الملغى: تحذف اللعبة التي أنشأها الاستيراد ولم يلمسها المستخدم، وتقلّم جلسات RA من
اللعبة التي أضافها بنفسه (بطرح ساعاتها لا بتصفيرها). وضعها داخل `migrateGamesList`
مقصود: يرث المسارين معًا فلا يمكن نسيان أحدهما. ترجع `{games, changed, purge}`،
وكل مسار يحذف أغلفة `purge.removed` ويعرض بانرًا بالعدد — **لا حذف صامت**.

---

## الميزات الحالية

### قاعدة الألعاب المحلية + معالج الإضافة

`window.GAMES_DB` مصفوفة `[name, platform, year, genre, developer, criticScore]` —
12,065 سجلًا من بيانات VGChartz+Metacritic، مفلترة لأجيال 4/6/7. تُفهرس مرة واحدة في
`GAMES_DB_BY_PLATFORM`، والبحث عبر `searchLocalGames(platform, query, limit)`.
**لا تُدخل هذه الألعاب تلقائيًا لمكتبة المستخدم** — اقتراحات تعبئة فقط.

المعالج (`GameModal` عند `isNew`) 3 خطوات عبر `wizardStep`:
1. اختيار الجهاز (من `PLATFORMS` مجمّعة بـ`FAMILIES`)
2. الاسم + اقتراحات فورية من القاعدة المحلية (بلا شبكة)
3. الملكية وخانة الأركيد (`renderOwnershipQuick` ← `renderOwned`)

**المعالج لا يسأل عن طريقة التشغيل** — ذاك سؤال وقت اللعب. `renderPlayback` حُذف.

آخر خطوة تستدعي `autoFill()` تلقائيًا — **جلب الغلاف والبيانات آخر إجراء** (مقصود).

### عالما المكتبة — أُسقطا بالمرحلة ٣ ⚠️

كان في التطبيق مُبدِّل نطاق (`ScopeBar`) يقسم كل الشاشات إلى «أجهزة أصلية» و«محاكاة»،
برتبتين مستقلّتين (`ORIGINAL_RANKS`/`EMULATOR_RANKS` عبر `worldRankOf`) وفلاتر تتبع
العالم المختار. **حُذف كله**: المُبدِّل و`SCOPES`/`inScope`/`scopeGames`/`scopeMeta`
والرتبتان و`KEY_SCOPE` وتنويه `.scope-note` و17 صنف CSS.

**لماذا سقط**: العوالم كانت مبنيّة على `g.hardware` بوصفه صفةً دائمة على اللعبة —
والصفة انتقلت إلى الجلسة، فاللعبة الواحدة قد تكون في العالمين معًا ولم يعد للقسمة
معنى. وحتى قبل ذلك كانت القسمة هزيلة: 142 لعبة مقابل 10، و110.7 ساعة مقابل 11.3،
فرتبة «عالم المحاكاة» تعيش على 9٪ من النشاط بسُلَّمٍ يبدأ من نفس الأرقام فتبدو
متجمّدة إلى الأبد.

**لا تُعِده.** إن احتجتَ تفرقةً بين طريقتَي اللعب فهي **فلتر** على الجلسات
(«لعبتها على محاكي») لا **عالمٌ** يقسم كل الشاشات ويضاعف الرتب والفلاتر — وذاك
بالضبط ما تحذّر منه القاعدة الذهبية 5.

**`rankOn(ladder, score)`** بقي وهو السُلَّم المشترك لكل الرتب المستقلّة (المِران،
الصيد، الرفّ، الأركيد، رتبة اللعب). كان منطقه منسوخًا حرفيًا في دالتين قبل أن
يُستخرَج. **أي رتبة جديدة تمرّ عبره** — لا تنسخه.

### نظام المِران — الأوسمة والدورة ووسم السَّحب

طبقة **محسوبة بالكامل** من البيانات الموجودة: لا حقول مخزَّنة ولا ترحيل.
ثلاثة مقاييس لا غير — **الوقت، والتختيم، وإعادة التختيم**. عدد ما تملكه لا يدخلها.

#### تصنيف اللعبة: تُختَم أم بلا نهاية

`isEndless(g)` يُشتقّ من `arcadeMode` أو نوع رياضة/حفلات، ويقبل تجاوزًا يدويًا
بحقل `endless` (`true`/`false`). فيفا والأركيد **لا نهاية لهما تُختَم**، فقياسهما
بالتختيم ظلم لهما — لهما مسار وسام موازٍ وقودُه الوقت.

#### وسام اللعبة — `gameMedal(g)`

| المسار | 🥉 | 🥈 | 🥇 | ✨ تذهيب |
|---|---|---|---|---|
| تُختَم | خُضتها · ٥س | أتممتها · تختيمة | أتقنتها · ٣ تختيمات | كل ٣ إضافية |
| بلا نهاية | خُضتها · ٥س | لازمتها · ٢٥س | أدمنتها · ١٠٠س | كل ١٠٠ ساعة |

المِران: `50 · 150 · 400` تراكميًا، و`300` لكل تذهيبة. **المساران متساويان في القيمة.**

#### دورة الإتقان — سبب العودة للعبة ختمتها

كل تختيمة تُغلق دورة وتفتح التالية، وساعات الدورة الجديدة تعطي مِرانًا أعلى:
`×1 → ×1.25 → ×1.5 …` بلا سقف (`CYCLE_STEP`).

⚠️ **`cycleHours(g)` تنسب كل جلسة لدورتها وقت وقوعها** — لا تضرب ساعات الماضي
بمضاعف الحاضر. من لعب 30 ساعة ثم ختم أمس لا تتضخّم ساعاته القديمة بأثر رجعي.
`cycleXpHours(g)` هي ما يدخل المِران. (كانت مكرَّرة في شاشة «مصادر نقاط الخبرة»
وقد سقطت تلك الشاشة مع نظام XP، فصار الموضع واحدًا.)

#### وسم السَّحب — `ghostTag(g, now)`

لعبة أعطيتها ساعةً فأكثر ثم لم تلمسها شهرًا. **اسم الوسم يتحدّد بكم أعطيتها لا بكم
غبت**: ساعتان تُنسى، وأربعون ساعة لا تُنسى.

| ساعاتك | بعد شهر | بعد ٦ أشهر | بعد سنة |
|---|---|---|---|
| ١–٣ | ما ضبطت | راحت بلا رجعة | نسيت إن عندك إياها |
| ٣–١٠ | الساحب | الساحب المزمن | سحبة العمر |
| ١٠–٣٠ | خيانة | خيانة موصوفة | دفنتها وهي حيّة |
| ٣٠+ | الخيانة العظمى | وكنتَ صاحبها | بيت مهجور |

**والوسم لا يُمحى بل يتحوّل** حين تعود وتختمها، حاملًا معه مدّة الغياب:
رجعت لها ← حبّيتها بعد ما سحبت عليها ← صالحتها بعد سنة ← قصّة حبّ متأخّرة.

ثلاثة قيود مقصودة:
- **الألعاب بلا نهاية لا تُوسَم** — لا سَحبَ على ما لا يُختَم.
- **الوسم حالة على البطاقة لا إشعار.** مكتبة بمئتَي لعبة كانت ستُطلق مئات
  الإشعارات أول تفعيل.
- **النبرة سخرية من الذات لا تأنيب**: «الساحب» لا «أنت مقصّر». الوعظ ثقيل.

#### العملة والرتب الثلاث

**نقاط المِران** (`miranOfGame` / `miranTotal`) = ساعات موزونة بدورتها ×10 + تختيمات
متصاعدة (`RUN_PTS`: 100·150·225·340·500 ثم 500 بلا حدّ) + أوسمة اللعبة + أوسمة الصيد
+ 1000 لكل لقب. **الاقتناء صفر** — اللعبة غير المُشغَّلة لا تعطي شيئًا مهما كان جهازها.

| الرتبة | المقياس | السُّلَّم |
|---|---|---|
| `miranRankOf` | نقاط المِران | 12 رتبة ثم ⭐ كل 25,000 ∞ |
| `shelfRankOf` | ساعاتك فيما لم تختمه | 8 رتب ثم ⭐ كل 750 ∞ |
| `huntRankOf` | نقاط إنجازات RA | 5 رتب ثم ⭐ كل 8,000 ∞ |

`endlessRank(ladder, score, step)` يبني على `rankOn` ويكمل بنجوم بعد آخر رتبة مسمّاة،
فلا يقف التقدّم أبدًا. **أي رتبة جديدة تمرّ عبره.**

`isPending(g)` = لعبة قصّة فيها ساعة فأكثر بلا تختيمة. **الألعاب بلا نهاية مستثناة**
— لا تأخير في ما لا يُختَم.

#### وسام الصيد ومساره المستقل

`huntMedal(g)`: فتحتَ الصيد → نصف الطريق → إتقان الطقم → إتقان صافٍ (كلها Hardcore)،
ويُذهَّب بإعادة الإتقان من `raHist.masteries`. **أوسمة الصيد وحدها تضيف مِرانًا، لا عدد
الإنجازات الخام** — RA يغطّي الريترو وحدها، و19 من 39 منصّة لا يدعمها، فمكافأة الرقم
بذاته كانت ستجعل لعبةً على PS5 أدنى منزلةً لسببٍ لا حيلة لها فيه.

#### الألقاب

16 لقبًا: 12 عامًّا + 4 للصيد (`TITLES` / `HUNT_TITLES` / `ALL_TITLES`).
**ولا واحد من الاثني عشر يذكر جهازًا ولا يحتاج حساب RA.** قلّتها مقصودة: نظام شخصي
بلا جمهور، فاللقب ما تُعرّف به نفسك، وندرته بالنسبة لمكتبتك هي ما يجعله يعني شيئًا.

#### البصمات ومكافأة الفداء

`FEATS` (18 بصمة) حدثٌ فريد يقع مرة. **وهنا وحدها يجوز الشرط**: قد تعتمد البصمة
على معدّل HowLongToBeat أو على RetroAchievements، بخلاف اللقب الذي يجب أن يكون في
متناول كل لاعب مهما كانت منصّته.

`ttbRatio(g)` = ساعاتك ÷ `ttbMain` — **المعيار العادل الوحيد** لمقارنة سونيك ١
بفاينل فانتسي: كلٌّ يُقاس بنفسه لا بغيره. سونيك في ساعة ونصف سريعٌ، وفاينل فانتسي
في ٤٠ ساعة سريعٌ كذلك. منه تأتي «⚡ أسرع من العالم» و«🔬 المُستقصي».

**`redeemBonus(g)`** يعطي 100/200/300/400 مِران حسب طول الهجر قبل التختيمة.
⚠️ كانت الواجهة تَعِد بمِران مضاعف والكود لا يعطيه — نصٌّ يَعِد بما لا يفعله الكود
لا يكشفه `node --check` ولا الكونسول، ولا يكشفه إلا اختبارٌ يقارن الرقمين
(خطأ 20 أدناه).
#### المِحكّات — خمسة محاور

`trialsOf(games)` يولّدها آليًا: **لعبة · نوع · جهاز · حقبة · صنف التختيمة**.
عتباتها `TRIAL_STEPS` ثم +500 بلا نهاية. **الجهاز والحقبة بالساعات وحدها** — عدد
الألعاب اقتناء لا لعب، وهذا جوهر ما يفرّق النظام الجديد عن القديم.

`runSizeOf(h)` يصنّف التختيمة: خاطفة (<3) · قصيرة · متوسطة · طويلة · ملحمة (60+).
**وصفٌ لا حكم** — أجهزة ما قبل بطارية الحفظ صُمّمت لتُختَم بجلسة واحدة، فقِصَر اللعبة
تصميمُ عصرها لا نقصٌ فيها. `beatInOneSitting(g)` يكشف مجدها من الجلسات نفسها.

#### المِران حلّ محلّ المستوى

الشريط العلوي وشريط النبض وكرت الجيمر وسجل الترقّي كلها تعرض **رتبة المِران** الآن،
ولم يبقَ في الواجهة أي `Lv.X`. تبويب «🎖️ المِران» هو الافتراضي.

**`miranTier(games)`** عدد صحيح تصاعدي يمثّل درجتك (1–12 للرتب المسمّاة ثم 13، 14…
لكل نجمة) — يحلّ محلّ رقم المستوى في `KEY_LEVELLOG` والاحتفال، فالمقارنة «هل
ارتقيتُ؟» تحتاج رقمًا لا اسمًا. و`miranTierName(tier)` هو الاتجاه المعاكس لعرض
إدخالات السجل القديمة.

⚠️ **النظام القديم حُذف بالكامل.** كان `levelInfo` و`totalXp` و`baseXp` و`RANKS`
و`GAME_RANKS` تعمل بجانب المِران وتُعرض في ثلاثة تبويبات «📜». حُذفت كلها:
المستوى وXP ونقاط الاقتناء وسُلَّم رتب اللعب وشاشة «📈 مصادر نقاط الخبرة»
وتبويبات الأرشيف الثلاثة.

**السبب**: كانت تعطي المستخدم هويّتين متنافستين في شاشتين — «ابن الصنعة 🧩
7,020 مِران» في الإنجازات و«صائد الجواهر — المستوى 8 — 7,832 XP» في الإحصائيات.
أيّهما أنت؟ سؤالٌ لا جواب له، وهو أصل الشعور بأن التطبيق «مشتّت».

**ما بقي من ذلك العالم**: `TRACKS` و`BADGES` و`SERIES` (المسارات والأوسمة
والسلاسل — ليست XP ولا تنافس المِران)، و`ARCADE_RANKS` (رتبة الأركيد على
الرئيسية)، و`GAME_TIER_AT`/`grankName` (وسام اللعبة على البطاقات). كلها
انتقلت إلى تبويب **«🎖️ المِران»** وهو الآن شاشة التقدّم الوحيدة.

**التحديات والسلسلة صارتا مِرانًا** بعد أن كانتا XP: `challengeMiran` و
`streakMiran` تدخلان `miranTotal`، لأنهما تقيسان **الانتظام** — محورًا لا
يقيسه مِران الألعاب (مئة ساعة في أسبوع ومئة على سنة سواء عنده).
⚠️ **مسار الموسم (`seasonMiran`) عرضٌ لا رصيد** — يقيس نشاط الربع بعملة
المِران ولا يُضاف إليه، وإلا دُفع مرّتين عن الساعة الواحدة (خطأ ١٤).

سجلّ الترقّي يخزّن **درجات المِران** ويُقرأ عبر `miranTierName`. رسائله
المخزَّنة تُعاد صياغتها بترميم لمرّة واحدة يحرسه `rec.fixedTop` — **رقمٌ لا
منطق**: ارفعه ليعاد الترميم على الأجهزة التي رمّمت بإصدار سابق.

#### ترتيب شاشة المِران — ثلاث طبقات مسمّاة

الشاشة تُقرأ من أعلى لأسفل كجواب على ثلاثة أسئلة، وكل طبقة لها عنوان:

| الطبقة | السؤال | ما فيها |
|---|---|---|
| الرتبة | **من أنت** | بطاقة رتبة المِران وحدها |
| الأرقام | **أين أنت** | 3 كبيرة (لعبة · مكتملة · ساعة) + 3 صغيرة في `.stat-strip` |
| 🎯 ما التالي | **ما الذي تلاحقه** | أقرب إنجاز · الموسم · تحديات الأسبوع والشهر |
| 🏅 ما أنجزته | **ما الذي بلغتَه** | سجل الترقّي · جدار الأوسمة |
| 📚 مكتبتك | — | التوزيع حسب العائلة |

⚠️ **ستة مربّعات متساوية تعني أن لا شيء منها مهم.** كانت الأرقام الستة بحجم
واحد، فانقسمت: أرقام مكتبتك كبيرة، وعدّادات التقدّم في شريط مضغوط (`.stat-strip`).

⚠️ **كانت شبكة الأرقام وبطاقة «أقرب إنجاز» بلا حارس تبويب**، فتظهران في كل
تبويبات شاشة الإنجازات — تحت قائمة التختيمات وقائمة الألعاب أيضًا. **أي عنصر
جديد في هذه الشاشة يبدأ بـ`tab === "…" &&`** وإلا تسرّب إلى الجميع بصمت.

#### الواجهة

تبويب **«🎖️ المِران»** في شاشة الإنجازات يجمعها كلها.
⚠️ `E` (اختصار `React.createElement`) يُعرَّف **بعد** `AchievementsView`، فالتبويب
يُعرّفه محليًا. لا تنقل الكتلة خارجها بلا انتباه لهذا.

**النظام القديم قائم كما هو** — المستوى والرتب الخمس والمسارات والأوسمة في تبويباتها.
لا شيء حُذف، فتقارن وتقرّر قبل أي استبدال.

### شاشة اللعبة — ما يظهر دائمًا وما يتبع التبويب

الترويسة الثابتة فوق التبويبات: **العدّاد ← الغلاف والبيانات ← أربعة أرقام ←
أزرار الحالة**. أزرار الحالة صعدت إلى هنا لأنها **تحكّم رئيسي** كان مدفونًا في
آخر تبويب «نظرة» تحت الوصف وويكيبيديا.

| التبويب | ما فيه |
|---|---|
| 🎮 نظرة | وقت التختيم · توقع الختم · الخطة القادمة · الملاحظة · الوصف وويكيبيديا |
| 🏁 تختيمات | التختيمات والأجزاء والأهداف · **البلاتينيوم الشخصي** |
| 🏅 إنجازات | لوحة RetroAchievements |
| 📓 جلسات | سجل الجلسات |
| 🧰 أدوات | تحديث البيانات · الغلاف واللقطات · **الألعاب الداخلية** |

⚠️ **التبويبات تعمل بـCSS لا بشرط JS**: `.dbody:not(.tab-X) :is(…){display:none}`.
عضوية العنصر في تبويب يحدّدها **اسم صنفه**، فنقلُ كتلة بين تبويبين = نقل صنفها
بين قوائم `:is(...)`. عنصر يحمل صنفين من قائمتين مختلفتين **لا يظهر أبدًا**
(خطأ ١٨). تحقّق دائمًا بـ`offsetHeight > 0` لا بوجوده في الشجرة.

⚠️ **صفر يُكتب صفرًا لا شرطة.** كانت عدّادات الجلسات والتختيمات تعرض «—» عند
الصفر — الشرطة هزّة كتف، والصفر معلومة.

### النقاط والرتب والإنجازات

- **المِران هو العملة الوحيدة.** `miranTotal` = مِران الألعاب + الألقاب +
  الانتظام (`challengeMiran` + `streakMiran`). لا نقاط اقتناء: **اللعبة غير
  المُشغَّلة لا تعطي شيئًا** مهما كان نوعها أو جهازها.
- `computeAchievements(games)` يبني `TRACKS` و`BADGES`. كل واحد دالة `check`/`metric`
  تفحص `games` من الصفر كل مرة — انتبه للأداء لو أضفت فحصًا متداخلًا.
- رتب مستقلة، كلها عبر `rankOn(ladder, score)`: `miranRankOf` (الرئيسية) •
  `shelfRankOf` • `huntRankOf` • `ARCADE_RANKS`.


### ألعاب الأركيد

`arcadeMode:true` → التختيم بالنتيجة لا بالساعات، و**تلقائي بالكامل**:
`applyArcadeAutoComplete(g)` ينشئ تختيمة كل `ARCADE_HOURS_PER_RUN` (3) ساعات، نتيجتها =
أعلى محاولة غير مُستهلَكة من `scoreAttempts` (تُعلَّم `used:true` لتختلف كل تختيمة عن
سابقتها). كل أزرار الاعتماد اليدوي **مخفية** لألعاب الأركيد.

**مربوطة بمكانين**: داخل `addPlayTime` و`handleSave` (ابحث عن `applyArcadeAutoComplete`).
أي دالة ثالثة تضيف ساعات للعبة أركيد يجب أن تمر عبرها.

`scoreAttempts` سجلّ منفصل تمامًا عن `playthroughs`: زر «📝 سجّل نتيجة» يعمل بلا تغيير
حالة اللعبة وبلا مِران، ويظهر أيضًا داخل عدّاد اللعب الحيّ (لتسجيل نتيجة وأنت تلعب).

الواجهة: شريط «🕹️ ألعاب الأركيد» بالرئيسية منفصل عن «لعبتها مؤخرًا»، ورتبة أركيد
مستقلة، و4 أوسمة، وtrack خاص.

### RetroAchievements — إنجازات فقط، بربط يدوي

**⚠️ RA لا يضيف ولا لعبة واحدة لمكتبتك، ولا يكتب أي وقت لعب، ولا يشغّل نفسه تلقائيًا.**
هذا شرط المستخدم الصريح، وقد كان الإصدار السابق يخالفه كله (راجع خطأ ١١ أدناه).
الإضافة يدوية من زر ＋، والساعات من العدّاد اليدوي وحده — **لكل لعبة بلا استثناء،
بما فيها المرتبطة بـRA**.

**الوصول**: مباشر من المتصفح بلا سيرفر وسيط — CORS مفتوح (`access-control-allow-origin: *`).
يحتاج اسم مستخدم + مفتاح API (مجانيان من إعدادات حساب RA)، يُحفظان محليًا
(`KEY_RA_USER`/`KEY_RA_KEY`).

| الوظيفة | الـEndpoint | ملاحظة |
|---|---|---|
| `raConsoles` | `GetConsoleIDs` | مخزَّنة مؤقتًا في IndexedDB |
| `raGames` | `GetGameList` | قائمة ألعاب منصّة واحدة، مخزَّنة مؤقتًا — أساس البحث اليدوي |
| `raGameProgress` | `GetGameInfoAndUserProgress` | **المصدر الوحيد** للإنجازات وحالتها |
| `raWorldRecord` | `GetGameLeaderboards` + `GetLeaderboardEntries` | الرقم القياسي (ميزة منفصلة) |

**الربط اليدوي** هو المكان **الوحيد** الذي يُكتب فيه `raGameId`:
`raOpenPick(game)` → يجلب قائمة ألعاب المنصّة → `raSearchGames` يبحث فيها محليًا →
المستخدم يختار من `RaLinkModal` → `raConfirmPick(raId)` → `raApplyAchievements`.
لا مطابقة تلقائية بالاسم إطلاقًا (خطأ ٦: المطابقة الجزئية تخلط الأجيال بصمت).

**`raApplyAchievements(localId, gid, prog)`** يكتب `ra` و`raHist` و`raGameId` **فقط**.
لا يلمس `hours` ولا `sessions` ولا `status` ولا `dateAdded`. هو المسار المشترك
الوحيد بين الربط الأول و«حدّث الإنجازات» و«حدّث الكل» — لا تكرّره في مكان رابع.

**التحديث يدوي دائمًا** — لا مزامنة تلقائية إطلاقًا:
- `raRefreshGame(id)` من تبويب «🏅 إنجازات» في صفحة اللعبة.
- **`raPullAll()`** من الإعدادات: يمرّ على كل ألعاب المحاكي، يحدّث المرتبطة،
  ويربط غير المرتبطة عند وجود **مطابق تامّ** لاسمها في قائمة منصّتها. تأخير
  260ms بين الطلبات احترامًا لحدود RA، وقوائم المنصّات مخزَّنة مؤقتًا فلا
  تُجلب إلا مرة واحدة لكل منصّة.

**`raExactMatches(list, name)` تامّة فقط عمدًا.** `raMatchGame` (المستخدَمة
للرقم القياسي) تقبل التطابق الجزئي، وهو يربط «Sonic» بـ«Sonic 3D Blast» بصمت
فيسحب إنجازات لعبة أخرى ويلوّث سجل التكرار. ما لا يُطابَق تمامًا يُعرَض بالاسم
في الإعدادات ليربطه المستخدم يدويًا. النسخ الفرعية (`[Subset]`/`~Hack~`) تأتي
بعد اللعبة الأساسية في الترتيب.

**واجهة الإنجازات في تبويب مستقل** (`tab-ra`) يظهر لألعاب المحاكي وحدها.
⚠️ تبويبات صفحة اللعبة تعمل بـCSS لا بشرط JS: `.dbody:not(.tab-X) .Y{display:none}`.
لوحة RA كانت تحمل `runs-box` فتختفي خارج تبويب التختيمات، وأزرارها `btn auto`
فتختفي خارج تبويب الأدوات — محبوسة في تبويبين معًا فلا تُرى أبدًا (خطأ 18).
**أي عنصر جديد في صفحة اللعبة: تحقّق من قواعد `.dbody:not(.tab-…)` قبل اختيار
أسماء أصنافه.**

**العرض لألعاب المحاكي فقط** — شرط المستخدم الصريح. البوابة الموحّدة هي
`raShows(g)` = `hardware === "emulator" && raGameId && ra`. لعبة على جهاز أصلي لا
تعرض إنجازات ولا تساهم في مسار أو وسام أو مِران، ولو بقي فيها `g.ra`.
**البوابة داخل `raGamePoints` نفسها** لا عند المستدعي، لأن `baseXp` يمرّ على كل
الألعاب ونسيان الفلترة هناك كان سيمنح نقاطًا للعبة على جهاز أصلي.

#### سجل تكرار الإنجازات (`raHist`) — نظام داخلي بالكامل

RA **لا يحفظ** تاريخ تكرار: لكل إنجاز `DateEarned` واحد يُستبدَل بصمت إن أعدت ضبط
تقدّمك وكسبته ثانيةً. فالمصدر الوحيد الممكن هو المقارنة المحلية بين كل مزامنة
والتي قبلها — ولهذا وُجد `raHist`.

```js
g.raHist = {
  v: 2,
  ach: { "<achId>": { times, hcTimes, dates: […], hcDates: […], title } },
  masteries: [{ at, awarded, total, hardcore }],   // كل مرة بلغت 100٪
  resets:    [{ at, from, to }],                   // متى انخفض عدد المحصَّل
  seededAt
}
```

`raMergeHistory(prevHist, prevRa, nextRa)` هو **المصدر الوحيد** لتحديثه:

- **مرة جديدة** = الإنجاز محصَّل الآن ويحمل **يومًا** لم نسجّله من قبل.
- **RA يحفظ تاريخين لكل إنجاز**: `DateEarned` (سوفت كور) و`DateEarnedHardcore`.
  كسبُه سوفت كور ثم إعادته هاردكور **كسبتان يعترف بهما RA نفسه**، ونعدّهما.
  ⚠️ لكن الفتح **المباشر** في هاردكور يضع التاريخين **متطابقين**، فعدّهما
  كسبتين يضاعف كل إنجاز هاردكور بلا سبب. لذلك نعدّ **الأيام المتمايزة** لا
  الحقول. `hcDates` تحفظ أي الأيام كانت هاردكور لعرضها بـ🔥.
- **إتقان جديد** = بلغتَ 100٪ ولم تكن كذلك، **أو** كنتَ متقنًا وظهرت تواريخ كسب
  جديدة (أعدت الضبط وأتقنتها بين مزامنتين فلم نر الانخفاض).
- **إعادة ضبط** تُسجَّل عند انخفاض `numAwarded`، و**لا تُنقص** أي عدّاد.
- **حدّ معروف وموثَّق**: تكرار الإنجاز في **نفس اليوم** لا يمكن تمييزه.
- **حدّ أعمق لا حيلة فيه**: إعادة تفعيل الإنجاز في المحاكي **بلا إعادة ضبط**
  لا تُسجَّل في RA إطلاقًا — الخادم يردّ «already unlocked» ولا يغيّر التاريخ،
  ولا يوجد في الـAPI أي سجلّ تاريخي للإنجاز. **لا يمكن لأي عميل اكتشافها.**
  الطريق الوحيد لعدّها هو عدّاد يدوي يضغطه المستخدم (غير مبنيّ حاليًا).
- السجل يبدأ من أول مزامنة بعد هذا الإصدار؛ ما قبله يُبذَّر كمرة واحدة.
- فكّ الارتباط (`raUnlink`) يحذف `ra` و`raGameId` و**يُبقي `raHist`** — تاريخك لا
  يُمحى، فإعادة الربط تُكمل العدّ من حيث وقف.

**اختبره دائمًا بالتشغيل مرتين على نفس البيانات**: `times` يجب ألا يتغيّر.
انجراف العدّاد هو الفشل الصامت الأخطر هنا.

#### الدمج مع نظام الإنجازات الداخلي

- **مسارات** (`TRACKS`): `ra_earned` • `ra_points` • `ra_master` • `ra_repeat`
- **أوسمة** (`BADGES`): `ra_first` • `ra_master_first` • `ra_master_twice` •
  `ra_thrice` • `ra_hundred` • `ra_hardcore`
- **النقاط**: `raGamePoints(g)` = نقاط كل إنجاز محصَّل + **نصف** نقاطه عن كل إعادة كسب،
  وتغذّي رتبة الصيد (`huntScore`) لا رصيد المِران.

### جلب البيانات والأغلفة

- `fetchGameEnrichment(name, key, platform, opts)` ينفّذ RAWG وويكيبيديا **بالتوازي**.
- `rawgFetch` **يفلتر بالمنصّة** عبر `rawgPlatMatches` (راجع خطأ ٦).
- الأغلفة الكلاسيكية من Libretro (`LR_SYS`) — غلاف علبة حقيقي مطابق للجهاز، لا لقطة شاشة.
- `bulkEnrich(all)`: `false` = إكمال الناقص، `true` = تحديث شامل يستبدل الأغلفة.
  تزامن 6 معًا (`BULK_CONCURRENCY`) مع **حفظ واحد في النهاية**.

### PWA والإشعارات — القيود الحقيقية

`sw.js` ملف حقيقي (لا Blob). يدير التخزين الأوفلاين + تذكير العدّاد كل 5 دقائق.

**كشف التحديثات — أهم ما في الـPWA عمليًا:**

⚠️ **`cache: "no-cache"` على طلب الشِل ضروري لا تجميلي.** GitHub Pages يرسل
`Cache-Control: max-age=600`، فـ`fetch(req)` العادي داخل الـSW يُخدَم من **كاش
المتصفح** قبل أن يصل الطلب للخادم — فيبقى المستخدم على HTML قديم عشر دقائق رغم أن
استراتيجية «الشبكة أولًا» تبدو صحيحة تمامًا في الكود. هذا كان السبب الجذري لخطأ ٢١،
وأعمق من غياب البانر.

بلا كشف تحديث يبقى المستخدم على نسخة قديمة محفوظة في التخزين المؤقّت **ويظنّ أن
ما نشرتَه لم يصل**. هذا وقع فعلًا (خطأ ٢١).

- `CACHE_VERSION` في `sw.js` — **ارفعه مع أي تغيير في الملفات المخزَّنة**. قاعدة
  `activate` تحذف كل كاش اسمه مختلف، فبلا تغيير الاسم لا يُحذف شيء أبدًا.
- **لا `skipWaiting()` في `install`** عمدًا: تفعيل النسخة الجديدة فورًا يعيد تحميل
  الصفحة بلا إذن، وقد يقع وأنت في منتصف تأكيد وقت لعب فيضيع ما أدخلته.
- الدورة: `updatefound` → `statechange` → `__swWaiting` → بانر `.sw-nag` →
  المستخدم يضغط → `applySwUpdate()` ترسل `skip-waiting` → `controllerchange` →
  إعادة تحميل **واحدة** (يحرسها علم `reloaded`).
- `reg.update()` يُستدعى عند الفتح وعند العودة للمقدّمة وكل 30 دقيقة — لأن
  GitHub Pages قد يخزّن `sw.js` نفسه دقائق.

**قيود لا يمكن تجاوزها بلا سيرفر — لا تعِد المستخدم بغيرها:**
- المتصفح قد **يُنهي SW خامل** فيُلغي أي `setTimeout` معلَّق. الجدولة المتسلسلة (5 دقائق
  كحد أقصى) أوثق من جدولة مسبقة طويلة (النسخة القديمة كانت تجدول 12 ساعة مقدَّمًا و**تفشل
  دائمًا تقريبًا**).
- `periodicSync` **لا يضمن** أي فاصل زمني؛ المتصفح يقرر.
- **الضمان الوحيد** لإشعار دقيق مع إغلاق التطبيق كليًا هو **سيرفر Web Push حقيقي**
  (VAPID) — **لم يُبنَ** (خارج نطاق استضافة ستاتيكية).
- المصدر الوحيد للإشعار الفعلي هو `sw.js`؛ الصفحة تعرض بانرًا داخليًا فقط. **لا تُنشئ
  نظام إشعار ثانيًا** — تكرار المنطق بمكانين كان سبب باگ «يرجع بعد 30 دقيقة بدل 5».

---

## ⚠️ أخطاء وقعت فيها فعلًا — لا تكرّرها

كلها حصلت في هذا المشروع، وأغلبها **فشل بصمت** بلا خطأ في الكونسول.

**١. تغيير enum بمكان واحد.** غُيّر `type` من `"disc"` لـ`"physical"` بلا تحديث كل
المقارنات الحرفية → حسابات XP خاطئة بصمت. **بعد أي تغيير enum: `grep` القيمة القديمة.**

**٢. الترحيل في مسار واحد.** `migrateGame` كان يُستدعى عند التحميل فقط لا عند الاستعادة →
استعادة نسخة أثناء الجلسة تُبقي البيانات بصيغة قديمة معطوبة.

**٣. قيمة enum يتيمة بلا واجهة.** إصلاح مؤقت ولّد `hardware:"nso"` وهي قيمة لا يطابقها أي
زر → تفتح اللعبة للتعديل فلا ترى أي خيار محدَّدًا. **وإصلاح السلوك الجديد لا يرمّم
البيانات المعطوبة أصلًا — أضف قاعدة ترميم في `migrateGame`.**

**٤. الوثوق بالتوثيق الرسمي.** توثيق RA يقول `UserTotalPlaytime` بالدقائق — **وهي
بالثواني**. سبّب مشكلتين معًا: ساعات ×60 (27 ساعة ظهرت 1636)، **و** عتبة الاستيراد قارنت
7200 ثانية بـ120 فدخل كل ما تجاوز دقيقتين (تضخّمت المكتبة لـ225 لعبة).
**تحقّق من الوحدات ببيانات حقيقية، لا بالتوثيق.**

**٥. `.track` حاوية أفقية.** `display:flex` صف — استخدامها لعناصر رأسية يسحق الأعمدة حتى
يظهر النص **حرفًا في كل سطر**. استخدم `.ragame` للحاويات العمودية.

**٦. المطابقة الجزئية للمنصّات.** `"playstation"` جزء من `"playstation 2"`، و`"xbox"` من
`"xbox 360"`، و`"wii"` من `"wii u"` → خلط الأجيال بصمت. لذلك:
- `rawgPlatMatches` (منصّتنا → نتائج RAWG): **مطابقة تامّة فقط** عبر `PLAT_ALIASES`
- `raPlatformOf` (اسم نظام RA → منصّتنا): جزئية بترتيب الأطول أولًا — **مقبولة هنا فقط**
  لأن أسماء أنظمة RA متمايزة كفاية، واختُبرت كلها
لا توحّد الدالتين، ولا تحوّل الأولى لجزئية.

**٧. مسارات تختيم مزدوجة.** التختيم له مساران: زر «🏆 مكتملة» و«🏁 اعتمدها كتختيمة» بعد
إيقاف العدّاد. دعم الأركيد أُضيف للأول ونُسي الثاني. **أي تعديل على منطق التختيم يجب أن
يطال المسارين** أو يُستخرج لدالة مشتركة (كما فُعل بـ`raApplyGame`).

**٨. `startViewTransition` بلا معالج رفض.** وعد `ready` **يُرفض** عند مقاطعة انتقال بآخر
→ `InvalidStateError` بكل تنقّل. غلّف أي استدعاء بـ`vtQuiet(t)`.

**٩. TDZ في مصفوفة اعتماديات `useCallback`.** وضع دالة معرَّفة لاحقًا في الـdeps يرمي خطأ
وقت الـrender (الـdeps تُقيَّم فورًا، بخلاف الجسم). راجع ترتيب التعريف داخل `App`.

**١٣. فتح الإنجازات مربوط بدوال بعينها بدل الحالة.** كانت `syncUnlocks` تُستدعى
يدويًا من `handleSave` و`handleDelete` فقط، بينما الطرق التي **تُفتح بها**
الإنجازات فعلًا — العدّاد، التختيمة من العدّاد، إضافة تختيمة، نتيجة أركيد، حذف
تختيمة — لا تستدعيها. النتيجة: لا احتفال عند الاستحقاق، و`KEY_UNLOCKED` يتقادم،
ثم تنفجر الاحتفالات المتأخّرة دفعةً عند أول تعديل لاحق لأي لعبة. الحل ربطها
بـ`useEffect` على `games` فتغطّي كل مسار حالي ومستقبلي. **القاعدة: أثرٌ يتبع
الحالة يُربَط بالحالة، لا يُستدعى من كل دالة.**

**١٤. مقياسان لنفس الشيء — وقع ثلاث مرّات.** أخطر نمط في هذا المشروع وأكثره
تكرارًا، وكله **يفشل بصمت**. المرّات الثلاث:

1. `gameScore` (رتبة اللعب) كان يقرأ حقل `hours` الخام الشامل للوقت التعويضي
   بينما `baseXp` يحسب الجلسات — فلعبة بساعة مسجّلة و39 مكتوبة يدويًا تعطي
   10 نقاط خبرة و40 نقطة لعب. وُحِّدا على `realPlayHours`.
2. مسار «⏱️ سيد الوقت» وكرت «ساعة لعب» كانا يقرآن `sumHours` (الحقل الخام)
   فيعرضان **138 ساعة** بينما XP والمِران والرتب تحسب `realPlayHours` = **122**:
   ستّ عشرة ساعة معروضة لا وجود لها في أي حساب. المصدر الآن `totalRealHours`
   وحده، و`sumHours` حُذف. **لا تقرأ `g.hours` لعرض ساعات المستخدم.**
3. `predictFinish` كان يتجاهل `ttbMain` ويخترع هدفًا من وسيط تختيماتك، فيظهر
   في نفس التبويب سطران متجاوران: «3س58د / 11س (36٪)» أي سبع ساعات متبقية،
   و«~2 س متبقية تقريبًا» — ثلاثة أضعاف الفرق. صار `ttbMain` هو الهدف حين
   يتوفّر، والوسيط بديلٌ عند غيابه لا منافسٌ له.

**أي رقم يُعرض للمستخدم في مكانين يجب أن يأتي من دالة واحدة.** ولا يكفي أن
تُصلح الموضع الذي رأيته — ابحث عن كل من يجيب عن نفس السؤال.


**١٥. مجموع البنود لا يساوي الإجمالي.** `totalXp` = الأساس + الرتب + التحديات +
السلسلة، لكن شاشة «📈 مصادر نقاط الخبرة» كانت تعرض أربعة بنود فقط، فيرى المستخدم
إجماليًا أكبر من مجموع ما أمامه بلا تفسير. أُضيف البندان وسطرُ إجمالي يكشف أي
فارق مستقبلي فورًا. **كل شاشة تفصيل يجب أن تجمع إلى الرقم الذي تشرحه.**

**١٦. استهلاك كل الموارد في أول دورة.** `applyArcadeAutoComplete` كان يعلّم كل
محاولات النتيجة `used` في أول دورة من حلقته، فإن تجاوزت 6 أو 9 ساعات دفعةً واحدة
خرجت التختيمة الثانية والثالثة **بنتيجة صفر**. صار يستهلك محاولة واحدة لكل تختيمة.

**١٧. خلط UTC بالتوقيت المحلي.** `monthChallenge` كان يقارن `toISOString().slice(0,7)`
بينما الأيام تُحسب بـ`localDay`. في الرياض (+3) جلسةُ الساعة 1 ليلًا من أول الشهر
تُخزَّن بطابع الشهر السابق فتختفي من تحدي الشهر. استخدم **`localMonth`** لأي مقارنة
شهرية: تقرأ نصّ اليوم كما هو وتحوّل طوابع ISO للمنطقة المحلية.

**٢١. نشرٌ لا يصل للمستخدم.** نُشرت أربعة تحديثات متتالية وتحقّقتُ من محتوى
الموقع الحيّ بـ`curl` في كل مرة — والمستخدم يرى النسخة القديمة، لأن الـService
Worker يقدّم المحفوظ ولا شيء يخبره أن نسخة جديدة وصلت. **`curl` يثبت أن الملف
على الخادم، لا أن المستخدم يراه.** التحقّق الصحيح يحمّل الصفحة في متصفح حقيقي
ويفحص ما تعرضه. وبنية التحديث نفسها (بانر + رفع CACHE_VERSION) هي الإصلاح الدائم.

**٢٠. نصّ يَعِد بما لا يفعله الكود.** كتبتُ في الواجهة «العودة للعبة هجرتها تعطي
مِرانًا مضاعفًا» وفي رسالة الكوميت مثلها، ثم لم أضف المكافأة إلى `miranOfGame`.
لا خطأ في الكونسول ولا في `node --check` — الوعد صحيح نحويًا وكاذب وظيفيًا.
**كل نصٍّ يَعِد المستخدم برقم يجب أن يقابله اختبار يقارن الرقمين فعلًا.**

**١٩. تطبيع نصفَي المقارنة بدالتين مختلفتين.** `raMatchConsoleId` كانت تطبّع اسم
جهاز RA بـ`lrNorm` (تُبقي المسافات) وتقارنه بأسماء مستعارة مكتوبة **بلا** مسافات،
فـ`"playstation 2".includes("playstation2")` ترجع false. النتيجة: **7 من 20 منصّة**
مدعومة في RA كانت تُبلَّغ «منصّة غير مدعومة» — PS2 وPSP وN64 وNDS وGBA وGame Boy
وAtari 2600. لا خطأ ولا استثناء، مجرد رسالة كاذبة.
الآن يُجرَّد الطرفان بـ`raKeyNorm`، **مع تطابق تامّ أولًا ثم احتواء مشروط بتطابق
الأرقام** — الأرقام هي ما يفرّق الأجيال، وبلا هذا الشرط يلتقط اسم «playstation»
جهازَ «playstation2» بصمت (خطأ 6 حرفيًا). **طبِّع طرفَي أي مقارنة بنفس الدالة.**

**١٨. أصناف CSS تخفي عنصرًا في تبويبين معًا.** تبويبات صفحة اللعبة تُظهر وتُخفي
بـCSS: `.dbody:not(.tab-runs) .runs-box{display:none}` و
`.dbody:not(.tab-tools) .btn.auto{display:none}`. بطاقة ربط RA وُلدت بـ
`className:"runs-box ra-box"` وزرّها `"btn auto"`، فصارت مرئية فقط في تبويب
التختيمات **و** تبويب الأدوات في آنٍ واحد — أي أبدًا. **لا خطأ في الكونسول ولا
في `node --check`؛ العنصر موجود في الـDOM بارتفاع صفر.** افحص قواعد الإخفاء قبل
تسمية أصناف أي عنصر جديد، وتحقّق بـ`offsetHeight > 0` لا بوجوده في الشجرة.

**١١. اختراع وقت لعب لم يجلس فيه المستخدم.** استيراد RA كان يستورد ألعاب الحساب
للمكتبة ويوزّع `UserTotalPlaytime` على أيام الإنجازات كجلسات `src:"ra"` تدخل كل
الإحصائيات وXP والسلاسل. النتيجة: مكتبة مليئة بألعاب لم يضفها أحد، وساعات لم
يلعبها أحد، ورتب مبنية عليها — **وعدّاد اللعب اليدوي مخفيّ** لتلك الألعاب بحجّة
أن وقتها «يُزامَن تلقائيًا». أُلغي كله. **القاعدة: مصدر خارجي يعطي إنجازات لا
ساعات؛ الوقت يسجّله المستخدم بنفسه.**

**١٢. قراءة state من دالة تحديث `setX(p => …)`.** في React 18 تُؤجَّل الدالة
للرِندر التالي، فإسناد قيمة منها لمتغيّر خارجي يُبقيه فارغًا. حصل في
`raConfirmPick`: توقّف الربط عند «جاري جلب إنجازاتك» بلا أي طلب شبكة وبلا خطأ في
الكونسول. **اقرأ من الإغلاق وضع القيمة في deps.**

**١٠. وسم بيانات حقيقية كـ`backfill`.** جلسات RA وُسمت `"backfill"` فاستُثنيت من كل
مواضع الفحص → ألعاب المحاكي أعطت صفرًا في كل الإحصائيات. الحل كان تغيير الوسم (`"ra"`) لا تعديل عشرات المواضع.

---
**٢٢. صنفان مختلفان بالاسم نفسه.** `.shelf-row` كان اسم رفوف المكتبة (شبكة
أفقية `grid-auto-columns:158px` بتمرير)، ثم أعطاه نظام المِران للقائمة العمودية
لرتبة الرفّ (`display:flex; width:100%`). القاعدة الأدنى تفوز، فتحوّل الرفّ إلى
flex وتقاسمت **ثماني** بطاقات عرض الشاشة — نص بحرف في كل سطر (خطأ ٥ بعينه، بلا
أي خطأ في الكونسول). ولاحظ أن الأعطال تتناسب مع العدد: رفّ بثلاث بطاقات يبدو
مقبولًا فيمرّ الخلل، وبثمانٍ ينهار. **افحص أن اسم الصنف غير مستعمل قبل أن تصف
به شيئًا** — `grep -c '\.your-class'` قبل الإضافة. (وقع مرّتين: هذه، و
`.stat-strip` الذي سرّب إطارًا على شريط أرقام شاشة اللعبة.)

**٢٣. علامة اقتباس مائلة داخل تعليق CSS.** كل الأنماط تعيش داخل قالب JS واحد
(`const CSS = ` … السطر 9409). كتابة علامة مائلة في تعليق CSS **تُنهي القالب**
فيصير ما بعده كودًا، و`node --check` **ينجح** لأن الناتج JS سليم نحويًا — لكن
نصف الأنماط تختفي. ونسيان `*/` يبتلع بقية الملف بالمثل. **لا مائلة ولا `${`
داخل هذا القالب، وأنهِ كل تعليق.** الفحص الحاسم ليس `node --check` بل عدّ
القواعد في المتصفح: `[...document.styleSheets].flatMap(s=>[...s.cssRules]).length`
— نقصانها يعني أن الأنماط انقطعت.


**٢٤. قياس التخطيط في لوحة متصفح مخفيّة.** فحصتُ أحجام الأزرار فوجدت أهدافًا
بـ13-23px وأبلغتُ عنها كخلل إتاحة — وكانت كلها وهمًا. اللوحة كانت مخفيّة:
 و، فانهار التخطيط، **وتجمّدت حركات
الظهور عند ** فبقيت  على  من .
الأحجام الحقيقية 26px و42px. **قبل أي قياس تخطيط: تحقّق من 
و** — وما يُقاس من DOM أو CSS (المعالجات، الألوان، الأسماء) يبقى
صحيحًا في اللوحة المخفيّة، أما العرض والارتفاع والموضع فلا.

## سجل التغييرات (موجز)

**تنظيف**: حذف كتالوج NSO كاملًا، وألعاب SEED التجريبية، واستيراد `NINTENDO_LIB` المرمّج.

**بنية البيانات**: نظام `hardware`/`type`/`displayType`/`emuDevice` الجديد + ترحيل شامل،
واعتبار NSO محاكاة، وترميم قيم يتيمة.

**ميزات جديدة**: قاعدة 12k لعبة + معالج إضافة 3 خطوات • نظام الأركيد كاملًا (تختيم تلقائي،
سجلّ نتائج، رتبة، أوسمة، شريط رئيسي) • تكامل RetroAchievements كاملًا (استيراد بالأوقات
والتواريخ الحقيقية، مزامنة تلقائية، عرض الإنجازات، الرقم القياسي العالمي).

**PWA**: تحويل من Blob لملفات حقيقية، وتذكير كل 5 دقائق بمصدر واحد.

**فصل السوفت كور عن الهاردكور (الأحدث)**: كان تاريخ السوفت كور يُرمى، فتضيع
الكسبة الأولى لمن كسب الإنجاز سوفت كور ثم أعاده هاردكور. صارا يُحفظان معًا
ويُعدّان كسبتين، مع حراسة الفتح المباشر في هاردكور من التضاعف.

**إصلاح مطابقة المنصّات**: 7 من 20 منصّة مدعومة كانت تُبلَّغ «غير مدعومة»
بسبب تطبيع مختلف لطرفَي المقارنة. أُصلحت مع حارس أرقام يمنع التقاط جيل بجيل آخر.

**سحب جماعي وتبويب إنجازات**: زر واحد في الإعدادات يمرّ على كل ألعاب
المحاكي فيحدّث المرتبطة ويربط ما له مطابق تامّ، ويعرض بالاسم ما يحتاج ربطًا
يدويًا. ولوحة الإنجازات انتقلت لتبويب مستقل في صفحة اللعبة بعد أن كانت مخفيّة
بقواعد CSS متعارضة.

**عالما المكتبة (أُضيف ثم أُسقط)**: مُبدِّل نطاق فصل الأجهزة الأصلية عن المحاكاة
عبر المكتبة والإحصائيات والإنجازات، برتبة مستقلّة لكل عالم. **حُذف كليًا في
المرحلة ٣ من فصل الجهاز عن الملكية** — بُني على `hardware` بوصفه صفةً دائمة على
اللعبة، والصفة صارت على الجلسة. بقي منه `rankOn` وحده وهو السُلَّم المشترك.

**حذف نظام المستوى القديم**: سقط `levelInfo`/`totalXp`/`baseXp`/`RANKS`/
`GAME_RANKS` ونقاط الاقتناء وشاشة «مصادر نقاط الخبرة» وتبويبات «📜» الثلاثة.
كان يعطي هويّتين متنافستين في شاشتين. المسارات والأوسمة والموسم والتحديات
انتقلت لتبويب المِران، والتحديات والسلسلة صارتا تعطيان مِرانًا لا XP.

**مراجعة شاملة وإصلاحات**: أُضيف `tests/run.cjs` (311 اختبارًا بلا
اعتماديات)، وأُصلحت سبع ثغرات منطقية: فتح الإنجازات لم يكن يعمل عند اللعب،
ورتبة اللعب كانت تحتسب وقتًا تعويضيًا يستثنيه XP، وشاشة مصادر الخبرة لم تكن
تجمع إلى الإجمالي، وتختيمات الأركيد كانت تخرج بنتيجة صفر عند القفز، وتحدي الشهر
كان يخلط UTC بالمحلي، وانهياران على بيانات تالفة. وصار تذكير العدّاد يستعيد
نفسه بعد إنهاء المتصفح لـSW. وحُذف كود NSO الميت.

**فصل RA عن المكتبة**: أُلغي استيراد الألعاب والأوقات والمزامنة التلقائية
كليًا. صار الربط يدويًا من صفحة اللعبة، والعدّاد يظهر لكل لعبة. أُضيف سجل تكرار
الإنجازات `raHist` (كم مرة كسبت الإنجاز، وكم مرة أتقنت اللعبة)، ودُمج مع النظام
الداخلي: 4 مسارات و6 أوسمة ونقاط XP. وتحذف الترحيلة بقايا الاستيراد القديم.

**دقّة وأداء**: فلترة RAWG بالمنصّة، تنفيذ متوازٍ، تزامن 6، حفظ واحد بالنهاية.

**إصلاحات**: وحدة وقت RA (ثوانٍ)، التكرار من النُسخ الفرعية، تواريخ الإضافة الحقيقية،
دمج المحاكي في الإحصائيات، `timer.startedAt`، عدّاد «X من Y»، ViewTransition.

## فجوات متبقية

- **سيرفر Web Push** — الفجوة الوحيدة الباقية لإشعار مضمون التوقيت **والتطبيق مغلق
  كليًا**. تحتاج VAPID وخادمًا يرسل، وهو خارج نطاق الاستضافة الستاتيكية. لا يمكن
  سدّها بحيلة في العميل — لا تَعِد المستخدم بغير ذلك.
  ما أُنجز بلا سيرفر: `rearmFromIDB` في `sw.js` تستعيد حالة العدّاد من IndexedDB
  عند كل إيقاظ لـSW (تفعيل، أول `fetch`، `periodicsync`، رسالة `wake` التي ترسلها
  الصفحة عند عودتها للمقدّمة). قبلها كانت `nagState` في الذاكرة فقط، فإنهاءُ
  المتصفح لـSW الخامل يقتل التذكير **نهائيًا** ولو عاد المستخدم للتطبيق.
- **الاختبارات تغطّي المنطق لا الواجهة** — `tests/run.cjs` لا يُرندر أي مكوّن.
  اختبار الواجهة ما زال يدويًا؛ يستاهل Playwright لو كبر المشروع.
- `RA_ALIASES` يحدّد أي منصّة يمكن ربطها بـRA (20 منصّة، كلها مُختبَرة في
  `tests/run.cjs`). منصّة غير مذكورة فيه تعطي «منصّة غير مدعومة» — وهذا صحيح
  لـSwitch وWii وXbox وPS3+ فـRA لا يدعمها فعلًا. الأسماء المستعارة تُجرَّد
  مسافاتها، فاكتبها بأي صيغة.
- سجل `raHist` يبدأ من أول مزامنة بعد هذا الإصدار؛ لا سبيل لاستعادة تكرارات سابقة
  لأن RA نفسه لا يحتفظ بها.
