# مكتبتي الجيمرية — دليل استكمال العمل لأي AI

هذا الملف موجّه لأي وكيل ذكاء اصطناعي (Claude، GPT، إلخ) يفتح هذا المستودع لأول مرة
ويحتاج يكمل التطوير. اقرأه كاملًا قبل أي تعديل — فيه قرارات معماريّة متعمّدة قد تبدو
غريبة لأول وهلة (مثل عدم وجود build step) لكنها مقصودة.

## نظرة عامة

تطبيق تتبّع مكتبة ألعاب فيديو شخصية (PWA)، عربي بالكامل RTL، **بدون سيرفر خلفي إطلاقًا**.
كل البيانات محلية على جهاز المستخدم (IndexedDB)، ويُنسخ احتياطيًا يدويًا كملف JSON.

- **لا يوجد build step.** الملف `index.html` يحتوي React 18 (UMD من cdnjs) + كل الكود
  مكتوب مباشرة بصيغة `React.createElement(...)` — **ليس JSX**. أي تعديل يُكتب مباشرة
  بهذه الصيغة، لا تحاول إدخال JSX أو أي أداة تحويل.
- **ملف واحد ضخم**: `index.html` (~8700 سطر JS داخل `<script>` واحد + CSS). نعم، طويل،
  لكن هذا مقصود لسهولة النقل/الاستضافة كملف مستقل.
- **تحقّق دائمًا من الصيغة بعد أي تعديل** قبل الانتقال لتعديل ثانٍ:
  ```bash
  awk '/^<script>$/{f=1;next}/<\/script>/{f=0}f' index.html > /tmp/x.js && node --check /tmp/x.js
  ```
  استخدم `awk` هذا وليس محاولة استخراج الكود يدويًا — أول `<script>` بلا سطر مستقل هو CDN، والسكربت الرئيسي هو أول `<script>` بمفرده بسطر منفصل.

## هيكل الملفات

| الملف | الدور |
|---|---|
| `index.html` | التطبيق كامل (React + منطق + CSS) |
| `games_db.js` | قاعدة بيانات محلية لـ12,065 لعبة (أجيال 4/6/7) للبحث الفوري عند الإضافة — راجع "قاعدة الألعاب المحلية" أدناه |
| `manifest.json` | PWA manifest حقيقي (ليس Blob) |
| `sw.js` | Service Worker حقيقي — تخزين أوفلاين + تذكير العدّاد بالخلفية |
| `icon.svg` | أيقونة التطبيق (نفس التصميم القديم، مُصدَّرة كملف حقيقي) |
| `AGENTS.md` | هذا الملف |

**لا يوجد** `package.json`، لا `node_modules`، لا خطوة بناء. الاختبار المحلي فقط عبر
سيرفر ستاتيك بسيط: `python3 -m http.server 8934` من مجلد المشروع.

## اختبار التغييرات

هذا المشروع بلا اختبارات آلية (no test suite). طريقة التحقق الوحيدة الموثوقة:
1. تحقّق من الصيغة بـ `node --check` (أعلاه) بعد كل تعديل.
2. شغّل سيرفر ستاتيك محلي وافتحه بمتصفح حقيقي (Playwright/Browser tool)، وليس
   `file://` مباشرة — بعض الميزات (Service Worker، IndexedDB مع بعض السياسات) تحتاج
   `http://`.
3. **لأي اختبار بيانات حقيقية**: استعمل تبويب الإعدادات → "📥 استعادة نسخة" → الصق نص
   JSON مباشرة في مربّع "أو الصق نص النسخة الاحتياطية هنا" وشغّل الفحص ثم التأكيد —
   أسهل وأوثق من محاكاة رفع ملف حقيقي عبر أتمتة المتصفح.
4. تحقّق من IndexedDB مباشرة عبر `indexedDB.open('gamelib_db', 1)` → object store اسمه
   `kv` → كل شيء مخزَّن بمفتاح نصّي (`gamelib:games`, `gamelib:timer`,
   `gamelib:cover:<id>`, إلخ) وقيمته **string JSON** (لازم `JSON.parse` بعد القراءة).

## نموذج بيانات اللعبة (Game object)

هذا هو الشكل الحالي بعد كل التعديلات في هذه الجلسة. أي حقل قديم غير موجود هنا **يجب
تمريره عبر `migrateGame()`** أولًا (راجع القسم التالي) قبل الاعتماد عليه.

```js
{
  id, name, platform,           // platform قيمة من مصفوفة PLATFORMS (له family + era)
  hardware: "original" | "emulator",
  emuDevice: "pc" | "steamdeck" | "aynthor" | "console" | "nso" | "other",  // فقط لو hardware==="emulator"
  emuDeviceOther,                // نص حر لو emuDevice==="other"
  displayType: "crt" | "hd" | "",// فقط لو hardware==="original" ومنصّة ريترو (era 1-7)
  status: "completed"|"playing"|"library"|"shelved"|"backlog",
  type: "physical" | "digital" | "downloaded" | "subscription",
  arcadeMode: boolean,           // جديد — يبدّل نظام "التختيم" لنظام نتيجة بدل ساعات
  nso: boolean, nsoSystem,       // "لعبتها عبر Nintendo Switch Online" — مستقل عن hardware لكن hardware يُفرض emulator+nso تلقائيًا لو nso=true
  genre, year, developer, remaster, rating, hours,
  dateAdded, dateCompleted, notes,
  playthroughs: [{ date, hours, n, score? }],  // score موجود فقط لو arcadeMode
  parts: [...], goals: [...],    // تختيمات فرعية لمجموعات/أهداف (لا تدعم arcade حاليًا)
  desc, wikiUrl, trailerId, trailerMp4, hasCover,
  crt: legacy — لا تكتبه بعد الآن، يُقرأ فقط للترحيل
}
```

### تاريخ الحقول (مهم لفهم البيانات القديمة)

- `type`: كان `"disc"|"digital"` قديمًا → صار `"physical"|"digital"|"downloaded"|"subscription"`.
- `hardware`: مرّ بعدة أشكال: `"original"|"emulator"` (الأقدم) → `"original"|"console_emu"|"pc_emu"|"nso"`
  (جلسة وسيطة، **لا تعيدها أبدًا**، `"nso"` كانت قيمة معطوبة بلا واجهة تطابقها) → الشكل
  الحالي `"original"|"emulator"` + `emuDevice` منفصل يحدد أي نوع محاكاة.
- `crt` (boolean) → استُبدل بـ `displayType`.
- **NSO ليست "جهاز أصلي"**: طلب المستخدم صراحة اعتبارها محاكاة (النظام الأصلي يُشغَّل عبر
  محاكي نينتندو الرسمي على Switch/Switch 2) — أي لعبة `nso===true` يجب أن تكون
  `hardware:"emulator", emuDevice:"nso"` دائمًا. لا تُرجعها لـ`"original"`.

## نظام الترحيل (migration) — لا تلمسه بدون فهم كامل

الدالتان `migrateGame(g)` و `migrateGamesList(list)` (قرب `reconcileGamesTime`، ابحث
عنهما بالاسم) هما **المصدر الوحيد** لتطبيع الحقول القديمة. **يجب استدعاؤهما من مكانين
اثنين معًا** — هذا كان مصدر باغ حقيقي هذه الجلسة:
1. `useEffect` عند تحميل التطبيق أول مرة (تحميل من `KEY_GAMES`).
2. `handleRestore()` — عند استعادة نسخة احتياطية (ملف/نص ملصوق/نسخة داخلية).

لو أضفت حقلًا جديدًا يحتاج ترحيلًا من صيغة قديمة، **أضفه في `migrateGame` فقط** وسيعمل
تلقائيًا بالمكانين. لا تكرّر منطق الترحيل في مكان ثالث.

## قاعدة الألعاب المحلية (`games_db.js`)

مصفوفة مسطّحة `window.GAMES_DB` — كل عنصر `[name, platform, year, genre, developer, criticScore]`.
مصدرها Kaggle/GitHub "Video Games Sales with Ratings" (VGChartz + Metacritic)، مفلترة
لمنصّات الجيل 4 و6 و7 فقط (SNES, Genesis, GB/Color, GameCube, PS2, Xbox الأصلي,
Dreamcast, Wii, DS, PS3, PSP, Xbox 360) — 12,065 سجل. تُبنى فهرسة `GAMES_DB_BY_PLATFORM`
مرة واحدة عند تحميل السكربت. الدالة `searchLocalGames(platform, query, limit)` تُستخدم
في معالج إضافة اللعبة الجديد (بحث فوري بدون إنترنت). **لا تُدخل هذه الألعاب تلقائيًا
لمكتبة المستخدم** — هي فقط اقتراحات تعبئة تلقائية عند الإضافة اليدوية.

لتوسيعها لأجيال/منصّات أخرى: نفس السكربت التحويلي (Python + `csv` module) الذي استُخدم
لإنشائها موجود في محضر هذه المحادثة، لا في المستودع — أعد بناءه من مصدر بيانات مشابه لو
احتجت التوسّع، بنفس تنسيق `[name, platform, year, genre, developer, score]`.

## معالج إضافة لعبة جديدة (الـwizard)

`GameModal` عند `isNew===true` يعرض 3 خطوات قبل النموذج الكامل (`wizardStep`
state، لا تخلط بينه وبين تعديل لعبة موجودة الذي يفتح النموذج الكامل مباشرة):
1. اختيار الجهاز (شبكة أزرار من `PLATFORMS` مجمّعة حسب `FAMILIES`).
2. كتابة الاسم + اقتراحات فورية من `searchLocalGames` (بدون شبكة).
3. طريقة التشغيل: أصلي (+CRT/HD لو ريترو) أو محاكي (+نوع الجهاز).

آخر خطوة تستدعي `finishWizard()` الذي يستدعي `autoFill()` تلقائيًا (نفس دالة "⚡ جلب
البيانات والغلاف" اليدوية) — **جلب الغلاف والبيانات يحصل تلقائيًا كخطوة أخيرة**، هذا
مقصود بطلب من المستخدم (سرعة أولًا، الغلاف آخر شيء).

الأغلفة للأنظمة الكلاسيكية (`LR_SYS` map) تُجلب من مستودعات Libretro-thumbnails على
GitHub (غلاف علبة حقيقي مطابق للجهاز، لا لقطة شاشة) — هذا موجود مسبقًا وليس من هذه
الجلسة، لكنه مهم لفهم لماذا الأغلفة تختلف حسب الجهاز.

## نظام النقاط/المستوى/الإنجازات

`baseXp(games)` هي دالة النقاط الأساسية. **أي حقل بيانات جديد يؤثر على النقاط لازم
يُحدَّث هنا وفي نسخته المكرَّرة داخل شاشة "📈 مصادر نقاط الخبرة"** (نفس المنطق مكرَّر
حرفيًا بمكانين — بحث عن `g.type === "physical" ? 10 : 5` يوريك الاثنين). كانت هذه
بالضبط الثغرة المكتشفة هذه الجلسة: تغيير قيمة enum بمكان واحد بدون تحديث كل مكان
يقارنها حرفيًا (strings) يفشل بصمت بدون أي خطأ في الكونسول — **راجع كل
`grep -n '"disc"'` / أي قيمة enum قديمة بعد أي تعديل على enum**.

`computeAchievements(games)` يبني كل الأوسمة/المسارات (`TRACKS`, `BADGES` arrays).
كل badge/track عبارة عن `check`/`metric` دالة تفحص `games` مباشرة — لا حالة مخزَّنة
منفصلة، يُعاد حسابها بالكامل من صفر كل مرة (بسيط لكن يعني أي فحص غير كفؤ (O(n²)) هنا
يتضاعف أثره — راجع الأداء لو أضفت فحصًا جديدًا يتضمّن `.find`/`.filter` متداخلة.

## تختيم ألعاب الأركيد (arcadeMode) — ميزة جديدة هذه الجلسة

بدل ساعات/HowLongToBeat، لعبة `arcadeMode:true` تُكمَّل بـ"أعلى نتيجة" (نفس مصفوفة
`playthroughs`، لكن كل عنصر فيه `score` بدل الاعتماد على `hours`، و`hours:0` دائمًا
حتى لا تتضخّم إحصائية ساعات اللعب بقيم النتيجة). **صُمِّمت عمدًا لإعادة استخدام كل بنية
`runsCountOf`/`gameRankInfo`/نظام الرتب اللانهائي/XP الموجود** — أي لعبة أركيد "تختيمة"
واحدة تُحتسب بالضبط مثل تختيمة عادية بدون أي تعديل إضافي في تلك الأنظمة. لو أردت توسيع
هذا (مثلًا: تتبّع 1-Credit-Clear بدل/بجانب النتيجة)، وسّع حقول عنصر `playthroughs`
نفسه، لا تبني نظامًا موازيًا.

**سجلّ المحاولات (`scoreAttempts`)** — أُضيف لاحقًا بطلب المستخدم: زر "📝 سجّل نتيجة"
منفصل تمامًا عن حالة اللعبة (`g.scoreAttempts: [{date, score}]`)، يُسجَّل بدون تغيير
الحالة ولا علاقة له بـ`playthroughs`/XP. عند فتح نموذج "🏆 مكتملة" لأي لعبة أركيد،
`openComplete()` يملأ حقل النتيجة تلقائيًا بأعلى قيمة من `scoreAttempts`
(قابل للتعديل يدويًا قبل الاعتماد) — هذا يحقق طلب المستخدم "أعلى نتيجة من كل
المحاولات تصير هي الرقم الرسمي للتختيمة". الدوال: `logScoreAttempt`/`removeScoreAttempt`
(تستخدم `mutateGame` الموجودة). لا تخلط بين هذا السجلّ و`playthroughs` — الأول مجرد
سجلّ محاولات بدون قيمة XP، والثاني هو التختيمة الرسمية التي تُحتسب في النظام.

**⚠️ فخّ حقيقي وقعت فيه**: التختيم له **مسارين منفصلين تمامًا** في الكود —
(1) `openComplete`/`confirmComplete` (زر "🏆 مكتملة" اليدوي) و
(2) `commitAsRun`/`completeFromTimer` (زر "🏁 اعتمدها كتختيمة" بعد إيقاف العدّاد).
أول تعديل لدعم الأركيد لمس المسار الأول فقط ونسي الثاني — فضل زر إيقاف العدّاد
يعتمد الساعات دائمًا حتى لو `arcadeMode:true`، بلا أي حقل نتيجة. **أي تعديل مستقبلي
على منطق التختيم لازم يُطبَّق على المسارين معًا** أو يُستخرَج لدالة مشتركة. الإصلاح
الحالي في `commitAsRun`: لو `arcadeMode && selPart < 0` يسجّل الساعة عبر
`onAddPlayTime` كالمعتاد ثم يفتح `openComplete()` (نموذج النتيجة) بدل استدعاء
`onCompleteTimer` مباشرة.

**خيار الأركيد في معالج الإضافة**: `renderOwnershipQuick()` (بجانب `renderPlayback()`)
يعرض أزرار نوع الملكية + خانة أركيد في خطوة "٣. طريقة التشغيل" بالمعالج — نسخة مختصرة
من قسم "النوع و الملكية" الكامل بالنموذج الكامل (بدون NSO/Game Pass/PS Plus/Remaster
عمدًا، تلك تبقى فقط بالنموذج الكامل بعد المعالج).

## PWA والإشعارات — القيود الحقيقية (اقرأ قبل الوعد بأي شيء للمستخدم)

`sw.js` يسجَّل كملف حقيقي (لا Blob كما كان سابقًا — كان هذا سبب جزء من مشاكل الموثوقية).
يدير: (1) تخزين مؤقت أوفلاين (2) تذكير العدّاد كل 5 دقائق عبر `setTimeout` متسلسل داخل
الـSW (3) `periodicSync` كمحاولة إضافية أفضل جهد.

**قيد حقيقي من المتصفح لا يمكن تجاوزه بدون سيرفر:**
- المتصفحات (Chrome/Android خصوصًا) قد **تُنهي Service Worker خامل** بعد ثوانٍ قليلة،
  مما يُلغي أي `setTimeout` معلَّق بداخله. جدولة متسلسلة (schedule-one-at-a-time، 5
  دقائق كحد أقصى انتظار) أكثر موثوقية من جدولة مسبقة طويلة (كانت بالكود القديم تجدول
  حتى 12 ساعة مقدَّمًا — هذا **فشل دائمًا تقريبًا** لأن أي timeout أطول من دقائق قليلة
  لا يبقى حيًا).
- `periodicSync` **لا يضمن** فاصل 5 دقائق حتى لو طلبته بـ`minInterval` — المتصفح هو من
  يقرر التوقيت الفعلي حسب "مستوى تفاعل" الموقع، وغالبًا أطول بكثير خصوصًا لتطبيق غير
  مثبَّت حديثًا.
- **الحل الوحيد المضمون لإشعارات دقيقة كل 5 دقائق حتى مع إغلاق التطبيق بالكامل من
  الذاكرة هو سيرفر Web Push حقيقي** (VAPID keys + endpoint يرسل push كل 5 دقائق أثناء
  عدّاد نشط). هذا **لم يُبنَ** في هذه الجلسة (يحتاج بنية تحتية خادم، خارج نطاق استضافة
  GitHub Pages الستاتيكية). إذا طلب المستخدم هذا مستقبلًا، أخبره بوضوح أنه يحتاج خادمًا
  (حتى لو Cloudflare Worker بسيط) قبل البدء — لا تعِد بشيء لا يمكن لصفحة ستاتيكية
  تحقيقه.
- تجنّب تكرار منطق الإشعار بمكانين مختلفين (الصفحة + الـSW) بنفس الوقت — كان هذا سبب
  باغ "يرجع بعد 30 دقيقة بدل 5" (نظامان متنافسان بدون تزامن). المصدر الوحيد الآن
  للإشعار الفعلي (نظام تشغيل + اهتزاز) هو `sw.js`؛ الصفحة تعرض فقط بانر داخلي
  (`tNag`) + اهتزاز فوري أثناء انفتاحها.

## سجل تغييرات هذه الجلسة (بالترتيب)

1. حذف كتالوج NSO بالكامل (NsoPicker + NSO_CATALOG + NSOView الميت).
2. حذف ميزة "تحديث بيانات كل الألعاب الناقصة" من الإعدادات (ثم **أُعيدت لاحقًا** بطلب
   المستخدم مع تأكيد أن الأغلفة تطابق الجهاز — راجع `bulkEnrich`).
3. حذف ألعاب SEED التجريبية (المكتبة تبدأ فارغة لمستخدم جديد).
4. بناء `games_db.js` (12,065 لعبة، أجيال 4/6/7) + معالج إضافة لعبة 3 خطوات جديد.
5. نظام hardware/type/displayType/emuDevice الجديد بالكامل (راجع "تاريخ الحقول" أعلاه).
6. حذف استيراد "مكتبة نينتندو" المرمّزة يدويًا (`NINTENDO_LIB`).
7. مراجعة شاملة لملفي نسخ احتياطية حقيقيين للمستخدم — اكتُشفت وأُصلحت 3 ثغرات:
   عداد "X من Y لعبة" الخاطئ، غياب الترحيل عند الاستعادة (لا التحميل الأول فقط)، وقيمة
   `hardware:"nso"` اليتيمة.
   Then correction: NSO يجب أن تكون emulator+nso لا original (بطلب المستخدم).
8. مراجعة نظام XP/الإنجازات — ثغرة `"disc"` القديمة بـ4 أماكن، أُصلحت.
9. ميزة تختيم أركيد جديدة (`arcadeMode` + نظام نتيجة).
10. تحويل PWA من Blob إلى ملفات حقيقية (`manifest.json`, `sw.js`, `icon.svg`) + إصلاح
    تردّد تذكير العدّاد (ساعة → 5 دقائق) + إصلاح باغ `timer.start` (كان يجب
    `timer.startedAt`) المسبِّب لعرض "NaN:NaN".
11. تختيم أركيد **تلقائي بالكامل**: `applyArcadeAutoComplete(g)` (قرب `migrateGame`)
    يفحص بعد كل تحديث ساعات — كل 3 ساعات (`ARCADE_HOURS_PER_RUN`) تُنشئ تختيمة جديدة
    آليًا، نتيجتها = أعلى محاولة من `scoreAttempts` غير المُستهلَكة منذ آخر تختيمة (تُعلَّم
    `used:true`). **مربوطة بمكانين**: `addPlayTime` و`handleSave` — أي دالة ثالثة تضيف
    ساعات للعبة أركيد لازم تمر عبرها أيضًا. أزرار الاعتماد اليدوي (🏆 مكتملة، ➕ أكملتها
    مرة أخرى، 🏁 اعتمدها كتختيمة) **مخفية بالكامل** لألعاب الأركيد الآن.
12. شريط "🕹️ ألعاب الأركيد" بالصفحة الرئيسية (منفصل عن "لعبتها مؤخراً") + رتبة أركيد
    مستقلة (`ARCADE_RANKS`/`arcadeRankOf`) مبنية على إجمالي التختيمات التلقائية + 4
    أوسمة و track جديد بنفس النمط الموجود.
13. تكامل RetroAchievements (أفضل جهد، غير مضمون): زر "🌍 جلب الرقم القياسي العالمي" في
    صفحة لعبة الأركيد. يتطلب اسم مستخدم + مفتاح API من إعدادات حساب RA (مجانيان، بدون
    سيرفر وسيط — تأكدت CORS تسمح بالطلب المباشر من المتصفح: `access-control-allow-origin: *`
    على `retroachievements.org/API/`). **لا يوجد endpoint بحث بالاسم** في RA، فالمطابقة
    تتم يدويًا: `raConsoles` يجلب قائمة الأنظمة (مخزَّنة مؤقتًا)، `raMatchConsoleId` يطابق
    اسم منصّتنا بـ`RA_ALIASES` (خريطة يدوية — وسّعها لو أضفت منصّات جديدة)، `raGames` يجلب
    كل ألعاب ذلك النظام (مخزَّنة مؤقتًا، قد تكون قائمة كبيرة)، `raMatchGame` يطابق الاسم
    (نفس `lrNorm`/`lrVariants` المستخدمة لأغلفة Libretro). **لم أُدرج جدول IDs الأنظمة
    الرقمية يدويًا عمدًا** — حاولت الحصول عليه من التوثيق العام فما لقيت مصدرًا موثوقًا
    100%، وتخمينها كان يعني بيانات خاطئة بصمت. بدلها اعتمدت على `API_GetConsoleIDs.php`
    الحي (يتطلب مفتاح لكنه يرجع القائمة الحقيقية) + مطابقة بالاسم — أبطأ قليلًا بأول
    استخدام لكل نظام، لكن صحيح دائمًا ولا يحتاج صيانة عند تحديثات RA.

14. **استيراد ألعاب وإنجازات RetroAchievements**:
    - `raUserGames(user,key)` → `API_GetUserCompletionProgress.php` (صفحات 500) يعطي قائمة
      ألعاب الحساب لكنه **لا يعطي وقت اللعب**.
    - `raGameProgress(gameId,user,key)` → `API_GetGameInfoAndUserProgress.php` وهو **المصدر
      الوحيد** لـ`UserTotalPlaytime` ولقائمة الإنجازات الكاملة مع `DateEarned` و`GuideURL`.
    - **⚠️ `UserTotalPlaytime` بالثواني لا بالدقائق** — التوثيق الرسمي يقول "minutes" وهو
      **خطأ**، تحقّقنا منه ببيانات حقيقية (لعبة بـ98,190 وحدة = 27 ساعة معقولة، لا 1636
      ساعة). تخزَّن الآن في `g.ra.playtimeSec` والتحويل `/3600`. الخطأ الأصلي سبّب مشكلتين
      معًا: ساعات متضخّمة ×60، **و** عتبة الاستيراد قارنت 7200 ثانية بـ120 فدخل كل ما
      تجاوز دقيقتين (تضخّمت المكتبة لـ225 لعبة). `migrateGame` فيه ترميم تلقائي يصحّح
      الساعات وجلسة الاستيراد معًا (يميّزها بـ`note === RA_IMPORT_NOTE`) ولا يلمس ساعات
      أدخلها المستخدم يدويًا، وفي الإعدادات زر لحذف ما دخل خطأً. لذلك الاستيراد يكلّف طلبًا لكل لعبة (مع تأخير 260ms احترامًا لحدود RA).
    - `raSync` في App: يستورد فقط ما تجاوز `RA_MIN_MINUTES` (120 دقيقة = ساعتان) كلعبة
      `hardware:"emulator"`. اللعبة الموجودة مسبقًا تُحدَّث إنجازاتها فقط، و`hours` تأخذ
      `Math.max` مع المسجَّل يدويًا حتى لا تُنقص بيانات المستخدم أبدًا.
    - البيانات تُخزَّن في `g.ra = {gameId,numAchievements,numAwarded,numAwardedHardcore,
      achievements[],guideUrl,syncedAt,playtimeMin}` و`g.raGameId` للربط/منع التكرار.
    - **شرط المستخدم الصريح**: كل ما يخص الإنجازات يظهر فقط عند
      `game.hardware === "emulator"`. لعبة على جهاز أصلي **لا تعرض أي شيء** عن الإنجازات
      حتى لو كان فيها `g.ra` من استيراد سابق — الشرط مطبَّق في مكانين: لوحة صفحة اللعبة،
      وفلتر تبويب "🏅 إنجازات RA" في الإحصائيات (وظهور التبويب نفسه).
    - **فخّ مطابقة المنصّات**: `raPlatformOf` يستخدم `raKeyNorm` (يزيل المسافات) + مطابقة
      تامة أولًا ثم جزئية **بترتيب الأطول فالأقصر**. بدون ذلك يلتقط `playstation` اسمَ
      "PlayStation 2" ويعطيه منصّة PS1، ويفشل "Nintendo 64"/"Game Boy Advance" تمامًا
      (اختُبرت كلها). أي منصّة جديدة تُضاف لـ`RA_ALIASES` تستفيد تلقائيًا.

15. **تحسينات استيراد RA (الجولة الثانية)**:
    - **التكرار**: RA يفهرس نُسخًا فرعية لنفس اللعبة (`[Subset - Bonus]`, `~Hack~`,
      `~Homebrew~`) بمعرّفات مختلفة، فكانت تُستورَد كألعاب مستقلة مكرّرة. الحل
      `raBaseTitle()` يجرّد هذه الوسوم و`raDedupKey(title, platform)` هو مفتاح المطابقة.
      عند مطابقة نسخة فرعية بلعبة موجودة تُدمَج الإنجازات باتحاد المعرّفات ويُجمَع وقت
      اللعب، بدل إنشاء بطاقة جديدة.
    - **التواريخ الحقيقية**: كانت كل لعبة تُضاف بتاريخ اليوم وجلسة واحدة.
      `raSessionsFromAchievements()` يبني جلسة لكل يوم حصلت فيه على إنجاز، ويوزّع وقت
      اللعب عليها بوزن عدد إنجازات كل يوم، فيصير `dateAdded` = أول يوم لعب فعلي
      و`lastPlayed` = آخر يوم. لعبة من 2019 تُسجَّل بـ2019. عند إعادة المزامنة تُستبدل
      جلسات الاستيراد فقط (تُميَّز بـ`note === RA_IMPORT_NOTE`) وتبقى جلسات المستخدم،
      ويُؤخذ التاريخ الأقدم دائمًا لـ`dateAdded`.
    - **⚠️ فخّ CSS**: `.track` قيمته `display:flex` **صف أفقي**. استخدامها كحاوية لعناصر
      رأسية يسحق الأعمدة حتى يظهر النص حرفًا في كل سطر (حصل فعليًا في تبويب إنجازات RA).
      استُبدلت بـ`.ragame` (flex column). لا تُعِد استخدام `.track` كحاوية عمودية.
    - تبويب إنجازات RA صار فيه فلاتر بالمنصّة وبنوع جهاز المحاكاة + ترتيب (الأكثر
      إنجازًا / نسبة الإكمال / الساعات / الاسم).

16. **دقّة المنصّة عند جلب البيانات + سرعة التحديث**:
    - `rawgFetch(name, key, platform, opts)` كان **بلا أي فلترة منصّة** — فيرجع أحيانًا
      نسخة PS1 للعبة على دريم كاست فتُجلب سنة/مطوّر/غلاف خاطئة. الآن يُرشَّح 12 نتيجة
      ويُفضَّل ما يصدر فعلًا على منصّة اللعبة عبر `rawgPlatMatches()`.
    - **⚠️ المطابقة تامّة فقط، لا احتواء جزئي**: جرّبت الاحتواء أولًا ففشل فشلًا صامتًا —
      `"playstation"` جزء من `"playstation 2"` و`"xbox"` جزء من `"xbox 360"` و`"wii"` جزء
      من `"wii u"`، فكانت الأجيال تختلط. `PLAT_ALIASES` تحوي أسماء RAWG الحرفية لكل
      منصّاتنا؛ أي منصّة جديدة تحتاج إضافة اسمها في RAWG هناك.
    - السرعة: `fetchGameEnrichment` صار ينفّذ RAWG وويكيبيديا **بالتوازي** بدل التتابع،
      و`bulkEnrich` صار بمجمّع تزامن (6 معًا) مع **حفظ واحد في النهاية** بدل حفظ المكتبة
      كاملة بعد كل لعبة (كان هذا أبطأ جزء مع مكتبة كبيرة). `bulkEnrich(true)` = تحديث
      شامل يستبدل حتى الأغلفة الموجودة؛ `bulkEnrich(false)` = إكمال الناقص فقط.
    - `applyCover` آمن للتزامن (تعديل `coversRef.current` جملة متزامنة واحدة بلا await
      بداخلها، و`setCovers` تحديث دالّي).
17. **عتبة استيراد RA صارت 30 دقيقة** (`RA_MIN_SECONDS = 30 * 60`).
18. **إصلاح Unhandled Rejection في ViewTransition**: `document.startViewTransition()`
    يرجع كائنًا وعوده (`ready` خصوصًا) **ترفض** عند مقاطعة انتقال بآخر. لم يكن أحد
    يعالج `ready` فكان كل تنقّل يسجّل `InvalidStateError` في الكونسول. `vtQuiet(t)` تبتلع
    الثلاثة (`ready`/`finished`/`updateCallbackDone`). أي استدعاء جديد لـ
    `startViewTransition` يجب أن يُغلَّف بها.

19. **⚠️ `src` الجلسة يقرّر دخولها في كل الإحصائيات**: يوجد **26 موضعًا** يفحص
    `s.src !== "backfill"` (XP، السلاسل، الخريطة الحرارية، عدد الجلسات، التحديات،
    الرحلة، الأوسمة…). كانت جلسات استيراد RA تُوسَم `"backfill"` فأعطت **صفرًا في كل
    شيء** — ألعاب المحاكي لم تظهر في أي إحصائية أو رتبة.
    - الحل: `RA_SESSION_SRC = "ra"`. بما أن `"ra" !== "backfill"` تدخل تلقائيًا في كل
      المواضع الـ26 **بلا تعديل أيٍّ منها** — لا تُضِف فحوصًا جديدة لكل موضع.
    - `migrateGame` يرحّل الجلسات القديمة (`note === RA_IMPORT_NOTE && src === "backfill"`)
      إلى `"ra"`، ويترك backfill الحقيقي (الناتج عن `reconcileGamesTime`) كما هو.
    - **دقّة دلالية مقصودة**: جلسة RA عندنا **مجموع يوم كامل** لا جلسة جلوس واحدة (نوزّع
      الوقت على الأيام بوزن الإنجازات). لذلك `isSitting(s)` تستثنيها من وسامَي "جلسة واحدة
      تجاوزت 3/5 ساعات" فقط، بينما تدخل عاديًا في وسام "6 ساعات في يوم واحد" وفي كل
      الإحصائيات اليومية. أي وسام جديد يعتمد على **طول جلسة واحدة** يجب أن يستخدم
      `isSitting` لا `src !== "backfill"`.
    - جلسات RA مثبَّتة على 18:00 محليًا (RA لا يعطي وقت اليوم)، فلا تفتح أوسمة
      "بومة الليل"/"طائر الفجر" بغير وجه حق.
    - التحديات الشهرية/الأسبوعية تفلتر بالتاريخ، فجلسات 2019 لا تُكملها خطأً.

## أشياء لاحظتها لكن لم ألمسها (فرصة لمن يكمل)

- `addFromNSO`/`fetchNSOCover` ما زالت موجودة في الكود لكن غير مستدعاة من أي مكان
  ظاهر بعد حذف NSOView — تحقّق قبل حذفها هل تُستخدم بمسار لم أراجعه.
- لا يوجد أي اختبار آلي (unit/e2e) — كل التحقق يدوي عبر المتصفح. لو المشروع كبر أكثر،
  يستاهل investment بسيط في Playwright.
- سيرفر Push حقيقي (راجع قسم PWA أعلاه) — أكبر فجوة متبقية لو المستخدم يصرّ على إشعار
  مضمون التوقيت حتى مع إغلاق التطبيق بالكامل.
